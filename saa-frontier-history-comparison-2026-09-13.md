# 可配置空间与有效前沿：历史业务语义复核

日期：2026-09-13。范围：当前工作区、Git 历史、真实浏览器，以及独立固定样本复现。只分析并记录，没有修改业务代码。

## 结论与前次报告更正

**原设计中的“多轮随机游走 → 按收益分桶 → 每桶保留最低风险点 → 作为下一轮起点”确实被改掉了。** 当前仍有随机采样、单项及联合约束修复、风险收益指标和多轮参数，但不再按每轮计算结果筛选下一轮起点。整条目标网格求解的恢复，没有同时恢复这套探索机制。

前次 `saa-taa-business-intent-recheck-2026-09-13.md` 对后续提交及 Dev 同步的核对，不能作为“当前实现完整保留最初设计”的结论；其中“保留可配置空间散点及代表点：保持”遗漏了轮次之间的选种语义。本报告补充、更正该范围判断，保留前次报告原文作为审核记录。

用户补充的权重精度要求也未被完整满足：**存在 0.1%、0.2%、0.5% 参数，不等于输出权重严格满足该刻度。** 历史版本本身也有归一化破坏精度的问题，不能按旧实现逐字恢复。

## 历史证据

| 时间 / Git 提交 | 经代码核实的变化 |
| --- | --- |
| 2025-10-10，`e8ac997`；2025-10-11，`a200bff` | 已有多轮游走、收益分桶选种、单资产与多资产联合边界、指标选项及权重量化。页面默认 6 轮，每轮样本、步长、分桶可配置。 |
| 2026-09-03，`7fdcc49afeabd338d79c73cf0383b4de62f39b92` | 此前审核引用的历史版本仍保留上述探索机制；`refine.count` 控制风险网格上的 SLSQP 求解数量。 |
| 2026-09-04，`88e4e75143163759de7084f5a0f935a0f6fad5b0` | NJIT 重构删除收益分桶选种；改成从最近 `buckets × 4` 个已接受候选中抽起点。参数名称保留，含义改变。 |
| 2026-09-11，`b3d4038` | 风险收益指标、多轮探索和权重量化控件放入默认关闭的“高级设置：指标口径与计算精度”。 |
| 2026-09-13，`c5719da` 及当前工作区 | 恢复独立的整条前沿目标网格；新增长期政策配置入口。历史实验仍在 `saa/allocation-lab`，但没有恢复原分桶选种。 |

上述为本地 Git 可验证的提交归因，不将提交日期等同于远端合并或上线日期。

## 逐项对比

| 业务能力 | 历史实现 | 当前事实与判断 |
| --- | --- | --- |
| 初始可行权重 | Dirichlet 随机向量；修复单项和总权重；联合约束不满足时拒绝、有限重试 | 首个候选固定从全 1 向量修复，其余随机生成；单项、联合及总权重仍检查。随机分布和接受规则并非逐字等价。 |
| 局部随机游走 | `w = (1-step) × seed + step × noise`，然后约束处理 | 仍有这一混合公式，但起点来源变了。 |
| 每轮向前沿筛选 | 按当轮收益范围等分桶，每桶保留最低风险候选，供下一轮选择；步长可逐轮缩小 | **缺失。** `buckets` 仅控制最近候选的回看数量，选起点不比较收益或风险，还可能使用同一轮刚生成的候选。 |
| 散点集合 | 初轮可行点，加后续各轮筛选保留的点 | 当前把所有接受点收集起来；默认 6 轮请求合计 20,000 次采样，不等价于原来的筛选后点集。点数更多不证明探索机制恢复。 |
| 单项与联合约束 | 修复单项后检验联合约束；不是对全部约束集合做严格最近点投影 | 当前迭代修复单项、联合与总权重并最终校验；这些能力没有整体删除。 |
| 自选风险、收益 | 页面可选，旧探索还用这些结果决定分桶与下一轮起点 | 页面和 API 仍支持；指标影响坐标、最终 Pareto、代表点及目标网格，但在有限有效指标值的复现中不改变随机阶段的权重轨迹。 |
| 指定权重精度 | 有 0.1%、0.2%、0.5%；先取整再归一化，可能破坏刻度 | 有同样选项；先取整再约束修复，同样可能破坏刻度。**严格精度要求未满足，属于存量缺陷。** |
| 整条前沿精炼 | 沿风险上限网格逐点最大化收益；使用所选指标、单项及联合约束 | 当前沿收益目标逐点最小化所选风险；点数与迭代预算独立。它是独立求解阶段，不能替代多轮探索。当前网格明确采用连续权重。 |

原实现的“逐步靠近”是筛选机制的意图，不是每轮每个点都更优或必然收敛的数学保证。

## 当前前端的实际位置

真实浏览器确认：[大类资产配置与策略回测](http://127.0.0.1:5173/pre-investment/saa/allocation-lab)。展开 **“高级设置：指标口径与计算精度”** 后可见：

- 收益：年化收益率、年化收益率均值、累计收益率、收益率均值、指数加权收益率；普通 / 对数收益类型。
- 风险：波动率、年化波动率、指数加权波动率、VaR、ES、最大回撤、下行波动率。
- 默认第 0–5 轮的样本点、步长、分桶，以及增加 / 删除后续轮次。
- 权重量化：不量化、0.1%、0.2%、0.5%。

单项和联合上下限位于未折叠的“大类资金边界”区域；单项列表须先加载方案。流程导航“长期配置 SAA”目前指向另一页 `saa/policy`，该页并不是原来的随机游走实验页。因此存在入口改变和关键控件默认隐藏两层可发现性问题，不能概括为“功能全部删除”。

## 独立复现

使用项目 Python 环境，当前直接执行 `backend.optimizer` 的固定签名 NJIT 内核；旧版从上述 Git 文件抽取原函数体在隔离参考命名空间执行。关闭局部精炼、目标网格和量化，以隔离随机探索行为。无真实投资数据、无网络计算请求、无结果发布。

### A. 改变风险指标是否改变后续探索

固定样本：`np.random.default_rng(137).normal([.0002,.0005,.0009], [.006,.012,.02], size=(252,3))`。收益为 `annual_mean`，年化 252；单项边界均 `[.05,.8]`，资产 0+1 合计 `[.3,.85]`；随机种子 42。

轮次参数 `(samples, step, buckets)`：`(300,.5,10)`、`(500,.25,10)`、`(500,.1,20)`。只将风险从 `annual_vol` 改成 `max_drawdown`。

| 检查 | 历史版本 | 当前版本 |
| --- | --- | --- |
| 返回探索权重数量 | 两次均 330 | 两次均 1300 |
| 两次完整权重数组是否相同 | **否** | **完全相同** |

当前 `explore_portfolios_kernel.nopython_signatures` 数量为 1，实际运行的是 NJIT。该复现证明所选风险已不参与轮间种子筛选；不表示风险指标对最终坐标或网格求解无效，也不据此断言每个样本上的最终前沿一定更差。

### B. 0.1% / 0.5% 是否约束最终权重

输入权重 `[.3334,.3334,.3332]`，每个资产边界 `[0,1]`，无联合约束。分别以 `.001` 和 `.005` 调用当前 `repair_weights_kernel` 及历史 `_quantize_weights`。

两种精度下，新旧代码均返回 `[1/3,1/3,1/3]`。当前返回状态为成功，但 `w / step` 并非整数：即最终约 33.333333%，不满足 0.1% 或 0.5% 刻度。这里存在严格可行的离散解，例如 0.1% 下 `[33.3%,33.3%,33.4%]`，0.5% 下 `[33.5%,33.5%,33.0%]`。

本轮被测 `backend/optimizer.py` SHA-256：`f189e2778231460b26e7926b494c1a70bbf977e4b8dea166dd90d700b21cbffd`。未重跑全量测试，不把历史回归通过等同于这些语义已满足。

## 应恢复的业务契约

1. 恢复逐轮收益分桶、桶内最低风险选种，并明确每轮输入种子来自哪里。`samples` 是采样预算，`step` 是扰动幅度，`buckets` 是收益分桶数量；三者不得改作其他含义。
2. 收益、风险、约束、轮次、权重精度属于探索的核心输入，必须归入“可配置空间与有效前沿”同一个功能块。用户在该块内完成设置、生成和查看结果；不能以“其他位置的高级设置仍保留参数”作为功能恢复的验收依据。该块须直接呈现收益和风险指标、单项上下限、联合上下限、每轮样本数／步长／分桶数、轮次增删、0.1%／0.2%／0.5% 权重精度，以及整条前沿精炼开关／20 或 200 个目标点／独立单点迭代预算。条件相关的细项可在块内按选择展开；生成按钮应明确使用本块展示的全部配置，结果留在同一功能块中。
3. 精度作为最终可采用权重的约束验收：`w_i = k_i × step`，同时满足总和 100%、每项上下限、每组上下限。修复后必须再次检查所有条件；没有离散可行解时明确失败，不能以归一化或连续修复悄悄放宽精度。
4. 连续理论前沿与指定精度的可采用组合应清楚区分。20 / 200 控制整条目标曲线的求解密度，不能代替随机探索轮次，也不能代替权重精度。
5. 保留已修复的最终 Pareto、代表点一致性和逐目标真实状态；用固定签名 NJIT 实现恢复后的语义。历史实现的分桶最大端点、量化破坏约束等边界问题需独立验证，不照搬旧缺陷。

源码定位：`backend/optimizer.py:265`（约束和取整修复）、`:480`（探索）、`:523`（桶数改作回看长度）、`:538`（当前选种）、`:565`（最后才计算前沿）；`frontend/src/pages/ClassAllocation.tsx:744`（完整请求）、`:941`（折叠入口）、`:1048`（轮次）、`:1079`（精度）；`frontend/src/components/frontier-grid/FrontierGrid.tsx:64`（连续权重约定）。

### 用户补充：功能块完整性

用户明确指出，问题是生成有效前沿的功能块内看不到上述配置，而不只是参数是否还存在。验收须从用户实际进入的生成区域检查：在同一功能块内找到、修改并提交全部核心参数，确认请求与设置一致；不得要求跳到另一页或寻找块外通用高级设置。此补充明确整改要求，尚未实施页面调整。

## 用户确认的整改边界：能力只增不减，前端模块统一

用户进一步明确：与原版相比，完善本功能应增加能力，不能减少原有能力；这些功能应位于同一前端模块。这是后续实现和验收的基线，不是可选的界面建议。

- 原有能力必须逐项保留或恢复：收益与风险口径、单项和联合边界、各轮样本／步长／收益桶数量、轮次增删、受约束游走及轮间择优选种、权重精度、可配置散点、整条前沿精炼与点数配置、候选权重查看。
- 同名参数改变含义不算保留；只在后端留参数、前端别处留入口，或用更多散点代替轮间择优，也不算恢复。
- 已有新增能力继续保留：单点迭代预算独立设置、每个网格目标的成功／失败与权重明细、候选采用、最终前沿和代表点的一致性检查。新增能力不能抵消旧能力缺失。
- 允许修复旧算法中的错误，但须保持用户要求的业务效果。尤其不能为了与旧实现一致，继续允许精度或约束失效；也不能以 NJIT 重构为由改变轮次、分桶等参数语义。源码仍保持唯一当前实现，不为保留能力复制旧实现。

前端统一归入“可配置空间与有效前沿”模块，建议内部顺序：

1. 数据方案、研究区间、收益／风险指标及相关口径。
2. 单产品／资产上下限、多产品／资产联合上下限。
3. 多轮随机探索：每轮样本数、步长、收益分桶数和轮次管理。
4. 最终权重精度：不量化、0.1%、0.2%、0.5%。
5. 整条前沿求解开关、目标点数及单点预算；代表点局部精炼作为额外选项。
6. 生成按钮、所用配置摘要、散点／前沿图、代表点、逐点权重及采用操作。

以上是同一个用户可见业务模块的内部组织，不要求将全部代码写进一个组件文件。可复用内部组件，但不得把配置和操作分散到不同业务入口。

本次补充重新核对当前源码；优化器哈希与上述独立复现一致。未重新运行该复现或全量测试，也未实施功能修复。
