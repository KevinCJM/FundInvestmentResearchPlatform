# 因子研究中心：按算法产物划分的双模块设计

日期：2026-09-06。范围：本地前后端开发、离线自动化验收；不自动提交、不改变真实研究发布及产品池、不下载外部数据。

## 后续增量：风格暴露与收益贡献

收益归因现已在同一套因子数据上增加固定/滚动暴露、逐日因子贡献、RF/截距/残差分列、财富加权的复利贡献串联，以及全区间/样本内/样本外/月度对账。页面提供暴露与贡献曲线、逐日估计证据及CSV/JSON导出。旧运行保持不变，缺少贡献数组时要求重新运行。PBSA和Brinson仍未纳入。

增量契约、实现文件及最新验收结果见《因子研究中心_暴露与收益贡献详细设计.md》。下面保留双模块基础版本的设计记录；涉及固定归因的原描述由本增量扩展，不表示仍只有静态回归。

## 1. 需求与概念

在原有入口 `/settings/factor-research` 内设置两个一级模块，不建立互不相通的两套系统。

| 模块 | 核心算法 | 主要产物 | 页面流程 |
| --- | --- | --- | --- |
| 产品特征因子 | 滚动特征 → 当期横截面标准化 → 对未来收益的截面检验 | 日期×产品×特征、分数、IC/RankIC、研究发布 | 因子库 → 研究检验 → 发布与应用 |
| 因子收益率 | 历史组合持有收益 / 双排序组合收益 → 因子收益率序列 | 日期×因子收益、来源与构造证据、回归暴露 | 构建工作台 → 收益率数据集 → 收益归因 |

区别不是投前/投后：两种产物都可服务产品尽调、配置和投后。评价方案依旧负责评价与准入；特征研究负责验证预测假设；收益率研究负责生产及检验解释变量。回归系数是暴露，不是因子收益率。Barra 风格横截面回归和 FF3 组合差额不是同一个构造算法；本次不冒称实现了完整 Barra 风险模型。

## 2. 现状与兼容

CodeGraph 已同步并读取：FactorResearchCenter → FactorResearchService → numba_kernels；现有服务已有版本化特征定义/研究、不可变运行、FF3 数据导入、RBSA/FF3 归因、产品池证据接口。

当前特征研究按完整 SSE 月末形成信号，IC 是每个信号时点的横截面相关，并非“IC 天生就是时间滚动回归”。新增采样频率（日/周/月）和滚动 IC 统计，旧方案缺少字段时仍取月频。历史运行原样读取；新能力不迁移或覆盖旧数组与发布。

保持现有 `/factors`、`/studies`、`/runs`、`/datasets`、`/attributions`、`/releases`、产品池 gateway 契约；新接口以 `/return-*` 命名。旧 `?run=` 深链接仍定位特征研究；一级模块选择采用 URL 查询参数，浏览器前进/后退可恢复。

## 3. 前端信息架构

顶部为两张可键盘操作的模块选择卡，文字直接说明“给产品生成特征和排名”与“生成因子收益并解释产品收益”。进入后显示各自二级功能，避免把因子库、归因、发布平铺成四个没有语义层级的入口。

特征模块保留现有编辑、运行、结果、发布组件。增加信号频率、滚动统计窗口与最少有效截面设置；结果同时显示逐期 IC 和滚动 IC/ICIR/正值比例。滚动统计以标签兑现日作为可知时点，不能将未来标签结果伪装成信号日当时可知的指标。样本内外分别滚动，跨界标签不参与。

收益率模块：
1. 构建工作台：选择算法、保存/修订方案、指定已有特征运行或上传的股票时点面板，运行后展示明确的收益率列、诊断、构造快照、缺失警告。切换方案后不显示不对应的旧结果。
2. 数据集：查看自行生成与外部导入的不可变数据集；显示方法、市场、币种、日期、有效样本；可预览和导出 JSON/CSV。FF3 导入继续兼容旧格式，通用格式显式列出因子名和总收益/超额收益回归口径。
3. 归因：复用 RBSA/FF3；增加通用因子收益回归。FF3 只允许 MKT_RF/SMB/HML/RF 齐备的同市场同币种数据，不能选择单一动量差额冒充 FF3。总收益口径的通用回归不将截距宣称为无风险调整 Alpha。

所有上传限制文件大小、严格校验；模板是空结构，不把示例数据默认为真实研究输入。计算忙时禁止重复提交，失败说明原因，空值显示“—”。表格仅局部滚动，模块卡片适配窄屏。

## 4. 新领域对象与存储

沿用 AtomicJsonStore、VersionStore 和 ArtifactStore；不引入数据库或第二套数据提供者。

- ReturnPlan：id/revision/name/method、source_run_id 或 source_panel_id、factor_key、quantiles、cost_bps、output_factor。方法为 characteristic_spread 或 ff3_2x3。方案编辑乐观锁冲突返回409；运行精确绑定指定 revision。
- FF3Source：市场、币种、真实来源、构造说明、交易日历、年度六月形成截面、每日股票总收益与前一交易日市值、每日 RF。作为不可变原始输入产物保存。上传不等于证明具备完整历史 PIT。
- ReturnDataset：kind=dataset，factor_names、dependent_return（total/excess）、frequency=daily、units=decimal_return、rows、source_method、构造方案快照/父运行/父面板标识、输入哈希、执行审计、诊断和警告。
- 行以 date 为唯一升序键，其余键必须严格等于 factor_names，excess 口径另有 RF；缺失为 null。不填零、不猜测百分数单位。
- ReturnRun 不再复制巨大收益数据：生成的数据集本身就是不可变运行产物，记录 return_plan_id/revision 及全部构造输入；方案列表与数据集列表区分。
- 特征收益构建从原运行冻结的 NPZ 读取 prices/days/decisions，不重读当前市场快照；读取前验证文件、dtype 和输入校验和。

## 5. 算法A：特征分组收益差额

输入为一个已完成的特征运行，选择其中一个标准化方向后的因子或综合分；不以样本外收益选择最优因子。对每个信号日可得截面按平均秩分组，固定末组为高分、首组为低分；并列不按代码随意拆分，任一端为空则明确不可用。

信号日 t → 下一交易日收盘建仓 → 随后逐日获得收益。各腿组内初始等权、持有期间按净值变化漂移，在下一明确调仓日重置。不把重叠 horizon 标签平均收益直接拼成因子收益。

`F_t = R_high,t - R_low,t`，多头名义1、空头名义1。分别保存高低腿收益、毛差额、双边换手、费用以及扣费差额诊断。默认因子数据列为毛差额，借券、融资、基金不可做空等执行限制单列警告；不能称为可实际交易基金组合。累计展示为收益率算术累计而不是可投资净值。

持仓发生缺失时该腿当日无效，不能按未来数据补齐或剔除缺失产品重配；在下一合法形成日可以重新定义下一持有段，但不掩盖中间断点。当前快照及固定样本池的修订/幸存者偏差随父运行传递。输出名不得使用 MKT_RF/SMB/HML/RF 冒充 FF3。

## 6. 算法B：FF3 风格 2×3 双排序

本机股票正式行情、复权和财务 PIT 接入尚不完整，继续关闭“从本地股票目录自动构建”的按钮。本次提供**基于显式上传的规范时点面板**的真实计算路径；没有原始数据时不给假结果。不是下载服务，不改变 Tushare 权限/频控协议。

输入形成截面：formation_date、asset、market_cap（六月形成日权益市值）、december_market_cap（上一年十二月权益市值）、book_equity（上一财年账面权益）、fiscal_year_end、announced_date、reference_member。权益与市值必须正；财年属于上一年度；账面权益公告日严格早于形成日。形成日必须是输入日历的六月最后交易日。每期独立使用参考池计算规模中位数、B/M 的30%及70%断点；B/M=book_equity/december_market_cap。

独立双排序形成 SL/SM/SH/BL/BM/BH 六组，每组按形成日市值加权，持有至下一年形成日，期间按总收益漂移。空组或不足断点参考样本失败关闭，不任意合并组。每日股票收益包含分红；退市损失应由输入总收益明确反映，不能静默删除退市行。

- SMB = (SL+SM+SH)/3 - (BL+BM+BH)/3。
- HML = (SH+BH)/2 - (SL+BL)/2。
- MKT_RF = 全部当日明确市场成员按前一交易日市值加权的总收益 - RF。
- RF 必须显式逐日提供小数收益，禁止默认为0或把年化利率直接塞入日收益。

六组每年的形成日期、断点、人数可审计。不能以未来收益/未来市值确定分组。已持有成员收益缺失导致相应腿与依赖因子 null，不把剩余股票重新归一化。方法标为 `FF3风格2×3（自定义市场与参考池）`；与 Kenneth French 官方美国数据的证券筛选、交易所断点、退市口径、数据版本不保证相同，不宣称官方复制品。市场集合的完整性和日历真实性由来源声明，不能从上传本身证明。

## 7. 收益率检验及归因

数据集诊断：逐列有效天数、均值、样本标准差、正收益比例、因子相关矩阵；展示原始逐日收益与算术累计路径。常数/缺失时相关性为null；不将统计均值/波动之比称为有无风险调整的 Sharpe。不提供未经多重检验校正的显著性结论。

FF3 和通用因子模型复用既有固定签名 NJIT OLS（带截距）；RBSA 保留非负、和为1约束。按日精确对齐，不前向填充。训练仅使用样本内数据，样本外固定系数评价；不足样本、秩亏返回明确状态。

## 8. API

原前缀 `/api/factor-research` 下新增：

| 方法与路径 | 行为 |
| --- | --- |
| GET /return-catalog | 模块/算法能力及数据契约、空上传模板 |
| GET/POST /return-plans | 方案列表/创建 |
| PUT /return-plans/{id} | 按 revision 更新 |
| POST /return-plans/{id}/runs | 固定 revision 运行并产出不可变数据集 |
| GET/POST /return-sources | 原始 FF3 时点面板目录/导入 |
| POST /return-datasets | 通用因子收益数据导入 |
| GET /return-datasets/{id} | 详情、原始行、诊断、血缘（含旧 FF3 兼容） |
| GET /return-datasets/{id}/export | CSV 导出，有限列名、固定日期，避免公式注入 |

GET /datasets 返回两种来源的统一目录；旧 POST /datasets 继续仅校验原 FF3 契约。特征 run、归因 artifact、return dataset 必须检查 kind，禁止跨类型误读。收益率数据集不允许走原产品评分发布/入池接口。

## 9. 计算架构与性能

`FactorResearchService` 保持现有对外门面；组合新的 `FactorReturnService`，后者独立持有收益方案和构造编排。新增 return_contracts.py、return_kernels.py、return_service.py，避免在既有特征 service 内堆完整新模型。

特征滚动统计、分组持有、权重漂移、FF3 断点/组合、收益诊断全部固定签名 Numba NJIT；业务层仅 I/O、校验、数组边界与封装。与现有主进程 readiness/并发限制共同预热，未就绪503、忙429；无请求新签名、无 Python fallback。float64/int64 C 连续数组统一边界。只复制需要冻结或重排的数组，不使用 pandas apply 数值循环。

JSON 输入有明确行数、资产数、交易日数与面板单元上限，避免上传造成无界内存；本版面向受控研究面板，大规模全市场多年分区导入需后续扩展，界面不隐瞒上限。

## 10. 验收矩阵

- 回归：原特征/归因/产品池测试、页面既有流程、构建。
- 数值：独立参考值对照；平均秩并列、空组、NaN/Inf、-100%收益、dtype拒绝、确定性、时滞、费用漂移。
- FF3：已知六组与独立公式；上一财年/公告/市值日期检查；空组失败；缺失持有腿不重配；年度重组生效次序。
- 时点：未来变动不改变历史分组；信号日不吃未来净值；IC滚动统计在标签兑现后可知且样本分界隔离。
- 服务：版本冲突、冻结输入哈希、kind隔离、市场币种/模型匹配、旧数据兼容、真实NJIT调用链。
- 前端：两级模块、返回/深链接、方案生成/结果切换、导入失败、数据集归因、空结果、不同屏宽无页面溢出。
- 本次不运行会写正式研究和产品池的 FACTOR_REAL_DATA=1 测试。

## 11. 方法来源

- Kenneth French: https://mba.tuck.dartmouth.edu/pages/Faculty/ken.french/Data_Library/f-f_factors.html （2026-09-06核对；SMB/HML六组公式与前期权益输入）。
- 既有项目设计《因子研究中心_详细设计.md》及 backend/factor_research/{service,numba_kernels,data,repository}.py 为旧契约事实来源。

## 12. 实施与验收记录（2026-09-06）

### 12.1 本地已落地

- 后端新增 `return_contracts.py`、`return_kernels.py`、`return_service.py`；原 `FactorResearchService` 通过组合方式提供收益率领域服务，复用不可变产物、启动预热和并发门禁。
- 两条真实数值路径：历史特征高低分组收益差额；上传股票时点面板的 FF3 风格2×3。不是仅调整页面名称。新增内核均固定签名 NJIT；离线接口测试验证了实际调度器调用、float64连续数组、冷启动503、忙429及无Python回退。
- 原特征方案兼容默认月频，新增日/周/月选择、逐期IC与标签兑现日滚动统计。旧运行不改写。
- 收益数据集支持外部导入、构建来源、日收益预览、相关性、算术累计、六组形成证据与毛收益/费用诊断；CSV先校验响应状态和类型再下载，避免把JSON错误保存为数据文件。
- 归因复用原RBSA/FF3并新增通用因子收益回归；市场、币种和列名不匹配时拒绝。总收益口径的截距不称为风险调整Alpha。归因快照保留不可变数据集ID、来源和哈希，不重复复制整份曲线、组合腿与成员明细。
- 前端仍位于 `/settings/factor-research`，两个一级模块各有三个二级入口；支持原 `?run=` 链接、模块/数据集查询参数、浏览器返回、冻结运行一键构建和结果一键归因。
- 原始NPZ输入和上传股票面板均校验哈希。缺失持仓不重配，部分建仓尚未形成完整多空对时不构造虚假过渡期收益。

### 12.2 已执行的自动化检查

| 检查 | 实际结果 |
| --- | --- |
| 原因子数值/服务、新收益率数值/服务、产品池后端测试 | 58项通过 |
| 因子原页面、新收益模块、App、ProductDetail前端测试 | 26项通过 |
| 原因子完整流程、特征收益构造与归因、FF3构造与归因；320/768/1440宽度 | 9项浏览器测试通过 |
| `npm run build` | 通过；原有大包及Browserslist版本警告仍存在 |
| `npx tsc --noEmit --pretty false` | 全项目未通过：其他模块已有类型问题；本次因子相关文件没有出现在最终错误列表 |
| CodeGraph sync | 已重新同步新增和修改代码 |
| Hermes evolve，显式选择本次15个新核心/测试/路由文件 | covered=15，uncovered=0，routing_only_violations=0 |
| Hermes validate | 未通过：因子模块原有及新增文件尚未被Git跟踪；没有为通过检查而自动暂存用户工作区 |

可重复执行的功能测试命令（Python须使用项目推荐3.12环境）：

```bash
python -m pytest backend/tests/test_factor_research_numba.py backend/tests/test_factor_research_service.py backend/tests/test_factor_return_kernels.py backend/tests/test_factor_return_service.py backend/tests/test_product_pools.py -q
cd frontend
npx vitest run src/pages/FactorResearchCenter.test.tsx src/pages/FactorReturnCenter.test.tsx src/App.test.tsx src/pages/ProductDetail.test.tsx --silent
npx playwright test e2e/factor-research.spec.ts e2e/factor-returns.spec.ts --workers=3
npm run build
```

数值对照包括独立六组公式、组内市值权重漂移、形成日先结算旧组合再建仓、未来变更不影响历史、并列空组、持仓缺失、-100%收益、滚动统计跨样本隔离、输入篡改拒绝与确定性。浏览器检查包括局部表格滚动、页面无横向溢出、无运行时异常、导出内容、模型自动衔接和错误上传提示。浏览器接口使用显式离线夹具；真实NJIT和HTTP契约另由隔离临时数据的后端测试覆盖，不能称为真实行情前后端联调验收。

### 12.3 保留边界

1. 未运行 `FACTOR_REAL_DATA=1`，未写正式研究、产品池或真实组合，未下载股票数据，未提交或暂存代码，未主动重启现有服务。
2. 本次证明的是受控输入上的算法与接口正确性，不是某个因子在真实市场中有效。FF3需要用户提供符合契约且来源可核查的股票时点面板；本地全市场自动构建仍禁用。
3. 当前FF3是自定义市场/参考池版本，不宣称完全复制Kenneth French官方证券范围、退市或历史数据版本。Barra横截面估计及完整协方差/特异风险模型未实现。
4. TypeScript全仓错误及Git跟踪门禁属于仍待处理的检查项；没有修改其他模块的代码或降低检查规则来掩盖它们。路由JSON为本地候选更新，相关新文件应在后续明确提交范围后统一纳入Git再通过稳定引用校验。
