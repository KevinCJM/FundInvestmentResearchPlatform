# 投前01–04：最终代码审核记录

日期：2026-09-14。范围：`ISSUE2609/BetterSaaTaa` 相对 `aa8ca7a8a707ed1cc10f4ef624f1ceb5894ebada` 的本需求变更。

本记录是代码自审核和自动测试证据，不是人类独立批准、生产发布或策略投资有效性认证。最终汇总见 [验收记录](bettersaataa-01-04-acceptance-2026-09-14.md)；各 M1/M2/M3 报告保留开发当时的观察，不以其中的历史待办替代本次结果。

## 开发中风险关闭

|编号|风险|最终处理与证据|结论|
|---|---|---|---|
|R01|风险预算新增评分改变原四候选随机序列。|唯一 `policy_candidates_with_budget_kernel`；省略预算仍保留四候选。原 Git 基线 3 个种子×2 组约束逐元素对照相同；`test_strategic_risk_budget.py`、原战略配置回归通过。|已关闭。|
|R02|CMA 子契约与主契约循环依赖。|公共类型下沉 `common_contracts.py`，实际 API 导入、启动与父契约校验均通过。|已关闭。|
|R03|BL/情景只显示结果，政策仍消费旧人工收益。|`cma_application` 冻结原始定义与有效收益/风险；政策、资金诊断和 TAA 门禁真实消费同一结果。`test_cma_model_integration.py` 与两类模型×3 屏宽真实 API 浏览器链路通过。|已关闭。|
|R04|费用分母随调仓模式悄悄改变。|独立 `cost_basis` 明示半数绝对权重变化或双边成交权重；旧请求保持原口径。时钟变化不隐式改变成本定义，独立财富推进参考和费用对账通过。|已关闭。|
|R05|目标权重合规掩盖非交易期实际持仓越界。|逐期检查交易前及期末实际持仓的资产、组别和偏离预算；输出越界期数并进入候选可行性及应用门禁。不使用未来收益提前修正。|已关闭。|
|R06|兼容日频接口额外复制完整扩展路径。|唯一 `_taa_recursive_kernel` 按模式直接分配所需输出宽度；薄适配直接返回原宽度连续数组。原 Git 基线 3 种子×3 费率、100期×4资产四组数组逐元素相同。|已关闭。|
|R07|新 NJIT 仅有装饰器，未进入预热或实际调用。|模型、机构、信号、时钟均纳入服务 warm/readiness 与 PID 门禁；禁止新签名。真实隔离 Uvicorn 启动、冷 PID 拒绝、只读/非连续输入和实际 dispatcher 测试通过。|已关闭；不据此声称正式部署已验证。|
|R08|只限制 TAA 导出，直接产品组合写入绕过新门禁。|共享 `portfolio_bridge.validate_decision_application` 调用 `validate_clock_application`，复核冻结政策、映射、实际持仓日期和预算。直接桥接及产品研究回归通过。|已关闭。|
|R09|战略映射重复复制历史面板。|显式 ID 换轴在原 I/O pivot 前执行，之后复用只读数组；不新建第二套完整历史矩阵。必要解码、过滤、对齐/pivot 分配如实记录。|已关闭；不宣称整个既有数据链零分配。|
|R10|新时钟下仍将固定目标压力工具称为策略回放。|情景输出明确 `current_target_fixed_stress`，当前目标、每观察期恢复目标及所选成本口径均可见；不冒充实际规则路径或历史部署。|已关闭。|

## 最终集成审核发现与修复

|编号|发现|修复与验证|
|---|---|---|
|R11|多信号二维方向校验在 Python 逐行调用 NJIT。|新增单一职责 `tilt_rows_status_kernel`，在一个已预热编译循环内复用原向量校验；兼容原容差，无新数据副本。`test_bettersaataa_final_audit.py` 覆盖零和、边界、NaN/Inf、只读 stride、owner 不污染和实际一次批量调用。|
|R12|政策/映射已到复核日时 `eligible=false`，却没有具体阻断原因。|资格与原因在同处构造；分别说明政策或映射过期，保留历史候选结果。补充政策、映射到期测试，候选页与确认页展示真实原因。|
|R13|战略先行已经能计算，但顶端旅程仍强制先有产品域，SAA 导航被禁用。|`StageLayout` 区分战略范围与原产品路径；无产品可进入 SAA，映射入口可返回；缺映射不开放 TAA 链接。补 2 项导航单测并由 320/768/1440 真实用户点击旅程进入 SAA。链接只表示可恢复入口，不替代后端应用资格。|
|R14|新浏览器模型用例填写已有值后采用精确 label 查找，在当前浏览器中超时。|改用具有明确 accessible name 的 textbox role；未修改业务逻辑迁就测试。两模型×3屏宽全部通过。|
|R15|用例看到 TAA 标题即结束，未等待其数据请求，出现 `route.fetch: Test ended`。|不吞异常、不跳过请求；改为等待真实预检、运行 TAA，核对冻结 CMA 风险和 `python_fallback=0`、缺实际持仓时不可交接，再验布局/对比度。|

## 提交593cb79复核后的修复

以下4项由后续独立复核发现，按人类“修改修复发现的问题”授权修复；不改写原开发阶段的R01–R15记录。本节修复完成时尚未提交或推送；后续提交前复核见验收记录第9节。

|编号|发现|修复与回归|
|---|---|---|
|R16|有效的长期组合信号被原全局31天默认值截成已过期；最新待执行信号的到期日也可能影响已采纳目标。|按实际采纳决策的分量有效期计算；覆盖原日频/独立时钟、明确到期/观察年龄/研究复核上限、到期当日、执行滞后及共享产品桥接。过期与缺产品域仍被拒绝。|
|R17|从TAA基线或历史版本恢复时，目标、战略范围和实施映射沿用另一项研究。|共用冻结基线引用转换，原产品基线明确清空不存在的战略引用；两类入口均验证返回02的实际URL。|
|R18|知识截止初始化取消映射历史GET，表单停在草稿且不会重读。|独立历史读取操作；保留只读覆盖证据、加载禁用、失败重试，旧响应不能覆盖后来版本或编辑。|
|R19|外部信号类型变化后，界面残留旧JSON，但请求已清空观察值。|按来源类型重新初始化日期输入，保留普通编辑中未完成的JSON；验证显示/请求一致、解析错误提示和计算禁用。|

本轮最终通过数量、命令和证据见[验收记录第8节](bettersaataa-01-04-acceptance-2026-09-14.md#8-提交593cb79复核后的修复验收)。

## 仍需诚实保留的边界

- 外部 Value/Carry/Macro/Sentiment 是有来源与日期的研究输入，不是本系统已经实现的数据下载或训练模型。
- 情景 CMA 是单期矩匹配；资金目标继续沿用已有模拟模型，不等于完整情景路径分布或随机负债 ALM。
- 显式现金角色与人工核验记录不是自动合规证明，不替代托管事实或外部审批。
- 决策/执行计划采用共同观察期，不认证真实交易所开盘、基金申赎或清算时点；无自动下单。
- 测试覆盖本需求及直接依赖的后端、全量前端与列明浏览器流程，不声称全仓所有后端测试或正式市场数据均已验收。
- 存量 Starlette/Pydantic、React act、Browserslist 和构建大包警告保留，不通过掩盖警告、升级无关依赖或放宽检查器来获得通过。
