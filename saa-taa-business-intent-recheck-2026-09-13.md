# SAA / TAA 业务意图复核

日期：2026-09-13。结论：**未发现后续提交或 Dev 同步引入新的业务意图偏离。**

## 核对版本与证据

- 现场读取 [PR #14](https://github.com/KevinCJM/FundInvestmentResearchPlatform/pull/14)：最新 HEAD 仍为 `2e5cdcfad4bb954f695f35b8266d68b9fc9c78ff`，没有上轮审核之后的新提交；Dev 基线仍为 `440cb42aa79bfdf90302c0555dcdb695f095a93a`。
- PR 范围内 56 个业务代码、测试及导航文件，与当前原始整改工作区逐字一致。Dev 同步保留业务逻辑，受影响的界面改动是样式和表格语义；共享 UI 与色板复用 Dev 的当前实现。
- 重新检查 Git 差异、CodeGraph 调用链、关键源码和测试断言。上轮测试记录的 970 项源码/配置哈希均未变化。
- 同一 HEAD 已有独立审核和后端 392、前端 924、真实 API 浏览器 6 项、旧 TAA 夹具浏览器 2 项通过的证据。本轮没有重复运行全量测试，不将旧结果称为本轮重跑。

## 业务意图逐项核对

| 原意图 | 当前事实 | 判断 |
| --- | --- | --- |
| 用 20 / 200 个点控制整条前沿的密度 | `point_count` 默认 20，范围 2–200；沿收益轴生成 N 个目标并逐目标求解。单点 `max_iterations` 默认 300，独立传参、保存。 | 保持 |
| 对每个收益目标寻找最小风险组合 | 先求最低风险和最高收益端点，再对每个目标最小化风险；资金、单资产及分组约束保留，收益采用不低于目标的约束。 | 保持 |
| 平滑来自更多真实求解组合 | 图表按真实求解坐标连接；失败处断开。`smooth=false` 关闭的是绘图库插值，点数增加仍会加密整条曲线。 | 保持 |
| 保留可配置空间散点及代表点 | 采样数量、三个代表点局部精炼、整条前沿网格分别配置；候选合并后统一筛选 Pareto 和代表点。 | 保持 |
| 选择前沿权重用于配置研究 | 成功网格点可采用为固定权重策略，再沿既有历史回测或 TAA 交接链使用；没有改成只显示图片。 | 保持 |
| 自上而下配置，TAA 受 SAA 约束 | 目标 → 大类 → CMA → 政策候选 → 确认采用 → TAA → 产品预算。冻结政策、CMA、资产映射和约束；TAA 前瞻主动风险及总波动门禁仍在。 | 保持 |

主要源码：

- [前沿设置与结果](frontend/src/components/frontier-grid/FrontierGrid.tsx)：默认值 10–12 行，参数 38–68 行，逐点采用 71–110 行。
- [历史配置页面](frontend/src/pages/ClassAllocation.tsx)：候选采用 717 行，网格请求 773 行，结果及曲线 1111 / 1164 行。
- [前沿数值流程](backend/optimizer.py)：逐目标求解 918 行，候选集合合并与重选 1514–1543 行。
- [政策采纳](backend/strategic_allocation/service.py)：248 行；[TAA 政策门禁](backend/strategic_allocation/policy_gate.py)：10 行。

## 需要准确理解的现有边界

1. 整条前沿求解默认未勾选。需在历史配置实验勾选“按目标网格加密整条前沿”，才会按默认 20 或设置的 200 个目标运行；默认 20 不等于打开页面自动运行。
2. 这次恢复的是历史配置实验的有效前沿。前瞻 CMA 政策页仍是有限候选比较；尚未把整条网格求解应用到前瞻 CMA 政策曲线上。若原意还包含这部分，则它仍未实现，不能把历史前沿恢复说成已经覆盖。
3. 实际求解器为固定签名 NJIT 主动集 QP / BFGS-SQP，保持逐目标最小风险的功能目标，但不是原 SciPy SLSQP 实现。非二次风险只提供局部数值结果。
4. 网格采用连续权重；同时开启散点取整需显式确认。增加目标点数不保证每个目标成功，也不保证每个目标都有不同的解。
5. 上轮用户接受的奇异协方差 P2 仍未修复，本轮没有将其重新列为新增问题。未完整新增验证“采用网格权重后完成策略回测”的真实浏览器闭环；源码采用链保持原样。

本轮只新增此复核记录，没有修改业务代码、原始审核报告或远端 PR。
