# 指标中心画布模式：实施与验收报告

日期：2026-09-07。

## 1. 交付结论

本地代码已实现可编辑指标画布，并替代旧的只读 DAG 展示。入口为“指标中心 → 定义与公式 → 画布构建”。保留构建向导和高级公式模式；默认入口仍沿用构建向导，不强制改变已有用户习惯。

“校验与预览”删除原 DAG 图、节点详情、拓扑表，以及对应的 ECharts DAG 数据加工逻辑，仅保留定义校验、产品/区间选择、真实计算结果与 Excel 导出。用于指标结果曲线的 ECharts 没有删除。

设计原文见同目录《指标中心_画布模式详细设计.md》。本报告中的实际实现说明优先于设计阶段的设想。

## 2. 页面实际布局

| 区域 | 实现 |
|---|---|
| 指标库 | 进入画布时收起，可随时展开；其他编辑模式保持原操作方式 |
| 基本信息 | 画布中折叠为名称、结果类型、版本摘要，按需展开 |
| 主工作区 | 画布占据公式区域，宽屏容器上限由 1440px 放宽至 1920px |
| 画布高度 | 常规桌面至少 560px，随视口调整；全屏使用浏览器可用区域 |
| 资源与属性 | 同时只打开一个面板；桌面为右侧栏，窄屏为可收起的底部面板 |
| 全屏 | 保留检查、应用和退出入口；Escape 退出，焦点恢复，背景禁止滚动 |
| 输出 | 标量单输出；时序 1–8 个通道，各通道可共享计算步骤 |
| 底部 | 输出选择、独立布局保存；完整公式和依赖信息折叠查看 |
| 快照配置 | 画布模式下移到计算编辑区域之后，不挤占主要工作区域 |

重要交互区别：选择和拖动节点只处理选择/布局；明确点击节点才打开参数设置，避免拖动时弹出面板遮挡画布。长计算链的自动排布始终从左向右，不沿用公共画布的五列折返布局。

窄屏使用节点列表及输入下拉框替代精细拖线；资源/属性面板按需展开，不要求横向滚动整个页面。

## 3. 已实现的构建操作

支持搜索输入数据和算子，点击添加、原生拖放、端口连线、下拉选择上游、直接输入固定常量、可选参数签名切换、步骤备注、复制、删除、撤销、重做、缩放、小地图及自动排布。

窗口长度、自由度等固定参数直接填写，不强制拆成常量节点。两个输入使用同一个上游时，仍保留两个命名参数，例如分子、分母不会合并。共享步骤会提示修改影响所有下游；只改一条分支可以复制后重连。

已有、兼容协议版本的标量指标可以锁定版本展开为独立计算步骤；这不是新增一个动态引用执行器。输入资源的可用状态沿用原指标元数据。

操作顺序为：修改画布 → 检查画布 → 应用到指标 → 保存或进入校验与预览。“检查”只报告结构/类型，不自动应用；“应用”写入指标草稿，不等于已经保存版本。

## 4. 后端设计与实现

### 唯一计算链

画布/向导/公式 → 规范表达式 → 现有 Typed DAG → 现有编译预热 → NJIT 数值计算。

新增代码不引入 Python 数值回退，不增加第二套公式引擎。正式保存、预览和 Excel 继续使用原指标服务。

### 新增接口

- `POST /api/custom-indicators/graph/resolve`：公式与编辑图双向转换、类型/版本检查、规范公式及定位诊断。公式来源与图来源互斥，回显草稿修订；不查询行情、不执行数值计算、不触发 NJIT 编译。
- `GET /api/custom-indicators/{id}/editor-state?revision=N`：读取与指标版本关联的布局。
- `PUT /api/custom-indicators/{id}/editor-state?revision=N`：以独立 `editor_revision` 乐观锁保存布局。图与已保存指标公式不一致、或布局版本冲突时拒绝保存。

编辑图采用 `graph_version=1`，最多 128 个计算节点、20 层嵌套、8 个输出；同时服从原表达式长度限制，标量 1000 字符、时序每通道 4000 字符。因此不是任何 128 节点的图都能无条件展开。

检查覆盖命名参数、循环、未知引用、孤立步骤、固定参数、非有限数值、输入输出类型和协议版本。布局存储使用独立 AtomicJsonStore，不增加指标定义修订，也不清空数值缓存。

### 文件边界

- `backend/custom_indicators/graph_contracts.py`：有界请求与存储协议。
- `backend/custom_indicators/graph_adapter.py`：编辑图到表达式的安全展开。
- `backend/custom_indicators/graph_service.py`：纯解析、类型映射与布局版本管理。
- `backend/services/custom_indicator_routes.py`：接口接入。
- `frontend/src/services/indicatorGraph.ts`：前端图协议与接口。
- `frontend/src/components/indicator-graph/`：图适配、编辑事务、属性编辑器及画布工作区。
- `frontend/src/pages/IndicatorStudio.tsx`：三种编辑入口、页面布局、保存及预览编排。
- 公共 `GraphCanvas` 只增加可选端口兼容、资源拖放、节点激活回调；历史情景和 ETL 的默认行为保持不变。

## 5. 自审核发现并修复的问题

1. 未应用图可能与旧公式并存：修改后立即使旧预览失效，阻止保存/预览/导出旧定义；切换编辑方式或指标时明确处理未保存修改。
2. 迟到请求可能覆盖新编辑：解析请求序号与取消信号保护；数值预览及 Excel 下载增加上下文校验，过时结果不再显示或下载。
3. 删除节点会残留布局键：状态变更时清理不属于当前图的坐标，避免后端拒绝有效布局。
4. 隐藏画布可能保存旧定义：通过当前公式来源标识校验，其他模式已修改公式时不保存过期图。
5. 新图首次加载时缩放不正确：图加载完成后再挂载画布；全屏和自动排布支持重新适配。
6. 异步检查导致焦点变化后 Escape 无效：全屏使用全局键盘退出与焦点约束。
7. 通道 ID 边输入边改导致属性编辑中断：提交输入时校验格式和唯一性，不在每个键击时替换通道标识。
8. 长链条排布折返，以及拖动弹出属性面板：改为单向层级排布，分离选择/移动与打开设置。
9. 输出元数据携带非编辑字段：按白名单投影，避免把推断结果等只读字段写入编辑协议。

## 6. 实际测试结果

| 测试范围 | 结果 |
|---|---|
| 相关后端回归 | 325 项通过 |
| 指标中心、图适配、历史情景、ETL 前端回归 | 80 项通过 |
| 使用真实指标 API 的浏览器验收 | 15 项通过 |
| 生产构建 `npm run build` | 通过 |
| 全项目 `tsc --noEmit` | 未通过，仍有既有类型错误 |

后端的 325 项包含新增图协议 20 项，以及原公式往返、Typed 编译器/运行时、产品服务、时序指标、标量/时序 Excel、指标路由和服务测试。纯解析测试用禁止编译的替身验证没有调用 NJIT 编译函数。

前端 80 项包含：图适配 10、编辑事务 7、原指标中心 40、原公式往返 3、历史情景画布 3、历史情景工作台 10、ETL 图适配 7。

浏览器使用隔离的临时指标仓库和真实 FastAPI 指标路由；没有写入用户的正式指标仓库。覆盖 320×800、768×1024、1440×1000。每种尺寸验证保存/重新载入、公式模式往返、多通道共享、资源增删及撤销；桌面和平板额外执行真实鼠标资源拖放和端口连线。已查看实际截图，不仅依据 DOM 判断布局。

浏览器测试没有连接生产行情源；真实行情数值和 Excel 兼容性由相关后端回归覆盖，不能把这次浏览器测试表述为生产行情端到端验收。

### 复现命令

项目根目录，以项目 Python 环境执行：

```bash
python -m pytest backend/tests/test_indicator_graph.py backend/tests/test_indicator_formula_roundtrip.py backend/tests/test_typed_indicator_compiler.py backend/tests/test_typed_indicator_runtime.py backend/tests/test_typed_indicator_product_service.py backend/tests/test_custom_indicator_time_series.py backend/tests/test_custom_indicator_time_series_excel.py backend/tests/test_custom_indicator_excel_export.py backend/tests/test_custom_indicator_routes.py backend/tests/test_custom_indicator_service.py -q
```

在 `frontend` 中执行：

```bash
npx vitest run src/components/indicator-graph src/components/computation-graph src/components/data-sources/etlGraphAdapter.test.ts src/pages/IndicatorStudio.test.tsx src/pages/IndicatorStudio.roundtrip.test.tsx src/pages/regime-workbench/RegimeGraphCanvas.test.tsx src/pages/HistoricalRegimeWorkbench.test.tsx --silent --reporter=dot
INDICATOR_TEST_PYTHON=/path/to/project/python npx playwright test e2e/indicator-canvas.spec.ts e2e/indicator-formula-roundtrip.spec.ts --workers=1
npm run build
npx tsc --noEmit --project tsconfig.json
```

## 7. 保留边界与未完成的项目级事项

- 全项目 TypeScript 检查仍有原有测试夹具、类型声明以及 `ResearchDataLab.tsx` 的空值检查问题。本次新增画布模块、接口封装、公共画布及 IndicatorStudio 生产代码没有出现在最终类型错误列表中；不能据此宣称全项目类型检查通过。
- 构建仍提示现有浏览器兼容数据陈旧和较大前端包；未扩大范围升级依赖或重构全站打包。
- CodeGraph 已同步并核对新链路。显式范围的路由覆盖检查报告 8 个新文件/共享画布路径尚未覆盖。新增源文件仍未 Git 跟踪，未将其强行提升为稳定路由事实，也未为通过检查而进行隐式暂存；应在后续纳入版本控制时同步路由文档。本次没有改动用户原有的路由 JSON 草稿。
- 没有实现中间节点真实数值调试、复杂子图模板或时序通道间递归引用。编辑图仍须无环；图表位置仍由使用指标的业务页面决定。
- 旧版 DSL 1.x 指标不会被画布静默升级，继续使用原公式编辑器。
- 本次完成本地开发与测试；未提交、推送或部署，未覆盖工作区原有未提交改动。

## 8. 中文显示修正（2026-09-07）

根据画布截图反馈，补齐节点摘要、端口提示、参数说明、上游选项、资源说明、数据依赖及输出维度的中文展示。新增 `indicatorGraphPresentation.ts` 作为显示层，变量与算子名称优先读取现有元数据，数据类型按结构和名义轴转换。不会用字符串替换改写公式、参数名、节点标识或保存的图；用户自己填写的指标名称和步骤备注也不改写。

示例：`returns` → 复权净值普通收益率；`periods_per_year` → 年化因子；`scalar` → 单个数值；`series<time>[T]` → 时间序列；`mask<time>[T]` → 时间条件序列（是／否）。通用向量显示为一维数组，只有明确按资产对齐时才显示资产向量；矩阵不再被误称为日期轴。上游下拉框下面补充可换行的完整已连接步骤说明，避免名称被截断后无法辨认。

本轮验证：相关前端测试 95 项通过（含新增中文显示测试 21 项），真实指标接口浏览器测试 12 项通过（其中中文验收在手机、平板、桌面各执行一次），生产构建通过。已查看实际生成的中文参数面板和数据依赖截图。浏览器用例确认前端传回的节点与绑定完全不变，数学排版、依赖和布尔类型保持一致；表达式的 `power(x, 2)` 与 `x ** 2` 由原解析器等价转换，不以源码拼写相同冒充语义校验。

全项目 `tsc --noEmit` 仍有既有错误，错误列表未涉及本轮新增或修改文件。显式范围 AI 路由检查仍有 6 个未登记路径；新画布文件未纳入 Git，未隐式暂存或修改其他任务的路由 JSON。后端代码没有修改，本轮未重跑数值与 Excel 全套后端测试；上文 325 项是此前画布交付的测试记录。

本轮复现命令（在 `frontend` 执行）：

```bash
npx vitest run src/components/indicator-graph src/utils/indicatorDiagnostics.test.ts src/pages/IndicatorStudio.test.tsx src/pages/IndicatorStudio.roundtrip.test.tsx src/pages/regime-workbench/RegimeGraphCanvas.test.tsx src/components/data-sources/etlGraphAdapter.test.ts --silent --reporter=dot
INDICATOR_TEST_PYTHON=/path/to/project/python npx playwright test e2e/indicator-canvas.spec.ts --workers=1
npm run build
```
