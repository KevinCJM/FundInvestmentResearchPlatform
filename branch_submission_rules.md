# 提交、审核与合并规范

本文件是分支流向、提交范围、Bot 审核和 Owner 例外的唯一规范来源。逐步执行说明见 [代码提交全流程](docs/submission-workflow.md)，GitHub 配置及其实际覆盖范围见 [分支保护实现](docs/branch-protection.md)。三份文档须保持一致。

## 1. 固定规则

1. 使用实际分支名 `main`、`Dev`，不得另建大小写不同的主线。
2. 所有人，包括 Owner，都禁止直接 push、强推或删除 main/Dev；变更必须通过 PR，使用 merge commit 合并。
3. main 只接受**本仓库 Dev → main**。开发分支先通过 PR 进入 Dev，再按需要发布到 main；回滚和历史回同步同样走 PR。
4. 普通协作者合并前必须有 **1 个有效 Approve**，由 PR 作者以外、有写入或管理权限的账号提供；新提交撤销旧批准。
5. **所有合入 Dev/main 的 PR 均须通过 Bot 审核，才能按正常流程合并。** AI 自审核、人工 Approve 和测试通过不能替代 Bot 审核。
6. **仅 Owner KevinCJM 可以在 PR 内豁免普通批准要求或例外合并未通过 Bot 审核的 PR。** 这不是所有 Admin 的通用权限，也不豁免 PR-only、禁止强推/删除、分支方向及其他必要检查。
7. **AI 代 Owner 绕过 Bot 审核前，必须先取得人类针对本次例外的明确允许，并在合并前回复 Bot。** 具体执行第 3 节；不得仅凭当前登录账号是 Owner、旧授权或“帮我合并”自行绕过。
8. 复用现有终端和 gh 授权。不增加用于区分 AI 与人类的账号、令牌、身份系统或技术授权服务；AI 必须主动遵守审批要求。

## 2. Bot 审核通过的判定

- 指定审核者为本仓库启用的 Codex 审核 Bot（GitHub API 登录名 `chatgpt-codex-connector[bot]`）。核验真实作者，其他账号模仿的文字、标签或反应不构成其结论。
- 审核必须对应 PR **当前最新完整 HEAD SHA**，并有可追溯的明确通过或无问题结论。记录 Bot 证据链接与提交 SHA。
- `Completed` 只表示执行完成。仍有问题建议的 `COMMENTED`、`CHANGES_REQUESTED`、人工关闭讨论、AI 回复“已修复”，均不等于 Bot 通过。
- 单独的点赞、无评论、绿色分支方向检查、未收到反对意见或经过一段等待，都不能推断通过。若 Bot 用点赞表达无问题，须同时核实其身份、对应本轮最新提交的完成记录及无问题结论；无法对应到本轮审核时继续等待或重新请求审核。
- 有问题时，逐条按当前代码、契约和测试判断。成立的问题修复并推送后，重新请求 Bot 审核最新提交；此前通过结论不能沿用。
- 未开始、进行中、服务失败、超时、结果不明确或只有旧提交的审核，一律视为**尚未通过**，默认等待或重试。不得虚构“不合理建议”作为绕过理由。
- 新意见出现后重新核对；实质问题未处理且没有有效 Owner 例外时，不得合并。AI 不能通过删除评论、撤销审核、修改规则或自行关闭讨论制造通过状态。

## 3. Owner 例外与 AI 的人类授权

Owner 本人保留例外合并权限。AI 使用 Owner 身份代为绕过 Bot 审核，必须按顺序完成：

1. **先核实。** 阅读每条相关意见及完整上下文。合理建议优先修复；不能把“实现麻烦”“已有测试通过”或“用户要求尽快合并”当作误报证据。
2. **准备可审核结果。** 向人类说明 PR、目标分支、完整 HEAD SHA、拟绕过的 Bot 意见链接、判断依据、相关代码/测试及剩余风险，并给出拟回复 Bot 的文本。
3. **明确请求并等待允许。** 人类必须明确同意本次绕过。提交/推送/普通合并授权、沉默、超时以及 AI 自审核均不代替该允许。已对同一提交和同一组意见明确授权的，不重复询问。
4. **先回复 Bot。** 获准后逐条在相应审核线程回复：原建议、为什么不合理或不适用于当前实现、代码和测试依据、此次人类授权及适用提交。合理建议若被人类明确决定暂时接受风险，必须如实写“接受风险”，不得说成误报。不能只在 PR 总结中写“已审核”。
5. **合并前再次核对。** HEAD 未变化，授权覆盖的意见未增加或变质，回复已成功发布，其他必要检查通过，来源及合并权限正确。新提交使原例外授权失效；新争议需要补充授权。目标分支变化导致实质合并差异时重新核验。
6. **按已授权范围执行。** 使用 `--match-head-commit` 锁定获准提交，通过 PR 执行 merge commit。不得临时放宽远端规则或扩大豁免账号。
7. **保留记录。** 在 PR 中留下自审核、逐条回复、授权依据与合并结果。若同意来自本次对话，由 AI 如实注明，不能冒充人类账号另发一条授权。

这项额外人类允许针对**绕过 Bot 审核**。Bot 已通过、仅使用既有 Owner 普通 Approve 豁免时，按已经获得的普通合并授权执行，无需再申请一次 Bot 例外。

## 4. 日常流程

1. 阅读 AGENTS.md、AI Hermes 路由、本文件及 [代码提交全流程](docs/submission-workflow.md)。
2. 检查工作区、当前身份和分支；fetch 最新 Dev/main。通常从 Dev 创建 `codex/<主题>`；用户明确指定当前开发分支时按其授权执行。
3. 完成授权范围内的变更、相关测试和文档同步。保护其他任务的脏文件；需要隔离时使用 worktree，不擅自 reset、clean 或 stash。
4. 自审核实际提交差异，只暂存本任务内容。共享文件须按实际改动拆分，不能整文件带入其他功能。
5. 在获准提交/推送的范围内执行 commit、push，并向 Dev 提 PR。正文写清目的、范围、验证、限制、需求文档和技术文档链接。
6. 读取 Bot 最新审核，按第 2 节判断；处理意见并复审，或按第 3 节获得有效例外。普通协作者同时满足人工批准要求。
7. 刷新 PR、最新 HEAD、目标基线、检查及权限；非 Draft、无冲突、Bot 已通过或有有效 Owner 例外，才能合并。
8. 合并后确认 PR 为 MERGED、目标分支正确，记录 merge commit，并核实远端目标包含该提交。
9. 发布另提 Dev → main PR，审核全部累积差异，重新满足 Bot 审核、必要测试及 `branch-policy`，不能用开发 PR 的审核代替发布 PR 审核。

## 5. 验证与权限边界

- 测试按实际变更范围执行；不把 pending、失败、未运行或历史结果写成当前通过。文档变更检查一致性、链接和路由，无须为此跑全量业务回归。
- 不提交密钥、正式数据、缓存或构建产物。自动测试使用可重复的本地夹具，不污染正式数据。
- 更新 AGENTS.md/路由时运行 AI Hermes validate，并通过 evolve 检查变更路径覆盖。
- 修改代码、写文档或制定政策，不自动授予提交、推送、合并或修改远端设置的权限。
- 本规范中的 Bot 合并门禁由执行者强制遵守。当前原生工作流 `branch-policy` 仅校验分支方向，三组 ruleset 不检查 Bot 内容；文档更新不等于已部署独立的 Bot required check。具体技术状态见 [分支保护实现](docs/branch-protection.md)。

## 6. 终端核查示例

```bash
# 核对身份、提交与检查；还须读取 Bot 评论、review 和行内线程。
gh api user --jq .login
gh pr view PR号 --json state,isDraft,headRefName,baseRefName,headRefOid,reviewDecision,statusCheckRollup
gh pr checks PR号
gh pr diff PR号

# 普通合并：已经满足 Bot 和适用的人工批准要求。
gh pr merge PR号 --merge --match-head-commit 完整HEAD_SHA

# Owner 普通批准豁免，或已完成第 3 节授权和逐条回复的 Bot 例外。
# --admin 本身不是授权，不能因命令可执行就跳过前述条件。
gh pr merge PR号 --merge --admin --match-head-commit 完整HEAD_SHA

gh pr view PR号 --json state,baseRefName,mergedAt,mergeCommit
```
