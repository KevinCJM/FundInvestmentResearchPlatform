# 指标中心画布模式：交互、接口与验收设计

日期：2026-09-07。范围：单产品自定义标量及直接时序指标；保留已有公式、向导、版本、预览和 Excel 合同。

## 1. 页面职责

“定义与公式”负责定义计算：“画布构建 / 构建向导 / 高级公式模式”是三种编辑入口。
“校验与预览”只负责检查定义、选择产品/区间/截止日、查看真实结果与下载 Excel。删除原来的 ECharts DAG、图例和拓扑表；不把同一图画两次。

画布本身是可编辑 DAG，而不是校验成功后的展示附件。未连完的图可以继续编辑，但不能保存旧公式冒充当前图。

## 2. 从人类操作出发的布局

### 常规模式

- 保留“定义与公式 / 校验与预览”一级页签，不让用户离开指标工作台。
- 画布模式下默认收起指标目录；提供明确的“展开指标库 / 收起指标库”。工作区取消原 1440px 的过窄上限，使用宽屏空间。
- 画布公式卡片排在最前；基本信息折叠为摘要（名称、标量/时序、版本），快照配置排在后面。向导和高级公式保留原有排列与操作习惯。
- 画布区域至少约 560px 高，随视口增高；工具条与状态紧邻画布，不要求反复滚动到页面顶部。
- 节点库与属性区是按需展开面板，不固定同时占据左右两侧。窄屏采用纵向表单与节点列表，禁止通过页面横向溢出来维持桌面布局。
- 全屏编辑把浏览器可用区域让给画布；保留指标名、检查/应用、退出入口。Escape 退出，恢复进入前焦点，锁住背景滚动，键盘焦点不逃离全屏区。

### 操作层次

1. 搜索变量、算子，点击添加；支持把资源拖到画布指定位置。
2. 点击节点，编辑参数；窗口、自由度等常量直接填表，不强制制造小节点。
3. 端口拖线与“选择上游节点”下拉框等价。既照顾熟练用户，也不要求精确鼠标操作。
4. 每个输入使用真实参数名；分子、分母不能因源节点相同而合并。
5. 最终输出明确指定来源；标量只有一个结果，时序支持 1–8 个通道，同一步骤可被多个输出复用。
6. 删除节点同时断开相关输入/输出，显示待连接；支持撤销、重做、复制、自动排布、缩放和小地图。
7. 轻量检查展示错误位置与类型；应用成功才更新正式公式草稿。未应用改动显著提示，并阻止保存/预览旧计算。
8. 公式、向导与画布切换是显式事务；未应用图不静默丢失。失败保留图，旧请求不能覆盖新编辑。

### 不做的事

不在指标定义中选择图表区域；不加入下载、训练、交易节点；不增加第二套数值执行器；不把能连线误当成产品数据一定可计算。

## 3. 前端模块边界

`IndicatorStudio` 负责指标目录、原有保存/校验/计算编排、页面布局和编辑模式。
`components/indicator-graph` 负责编辑图适配、节点库、参数检查器、输出管理、撤销与全屏。
`services/indicatorGraph` 负责轻量解析与编辑状态 API。
公共 `GraphCanvas` 只增加可选端口兼容和资源拖放扩展，历史情景与 ETL 默认行为保持不变。

编辑节点使用稳定字符串 ID；公共画布 schema 可以按实例 ID 构建，不把重载算子固定成一种输入输出类型。图中的 named arguments 是连接真相，edges 由其派生。

图形布局变化不使数学结果失效。结构/参数变化立即使当前预览失效，应用成功后沿用原 validate/compile/evaluate 链。

## 4. 后端协议

### 编辑图

`graph_version=1`，最多 128 个计算节点、深度 20。节点分 variable、constant、operator。算子输入为命名参数：`{source:'node', node_id}` 或 `{source:'constant', value}`。不允许任意 Python 或动态代码。

输出为具名根节点，输出 ID 唯一；时序的显示元数据不决定图表放置位置。

### POST /api/custom-indicators/graph/resolve

请求包含协议上下文、`draft_revision`，及互斥来源：formula（expressions）或 graph（graph）。

返回：valid、对应草稿修订、规范 expressions、可编辑 LaTeX、展示公式、编辑图、推断节点类型、结构指纹、定位诊断。

该接口纯解析：不读取行情、不编译 NJIT、不执行数值计算、不写运行缓存。复用现有 Typed DSL 对名义轴、类型、量纲、口径、白名单、版本及根输出进行检查。图展开严格限制源码长度，拒绝环、未知引用、缺参、孤立节点与非法固定参数。

解析成功不等于已准备好运行；正式预览/保存仍调用现有校验准备流程，计算使用已有预热 NJIT 计划。

### GET/PUT /api/custom-indicators/{id}/editor-state?revision=N

独立存储图形布局与可重建图缓存，使用 AtomicJsonStore 与独立 editor_revision 乐观锁。服务端将缓存图解析结果与锁定指标版本的规范表达式对比；不同则拒绝，不允许图覆盖正式定义。

布局保存不增加指标定义修订，不清空计算缓存。指标保存成功、布局保存失败时明确区分，不伪称两者是跨文件事务。旧版不支持画布时保留原编辑器，不自动升级 DSL。

## 5. 安全与异步

- 每次编辑增加请求序号；过时解析响应忽略。应用或离开模式前检查当前图是否仍与解析结果一致。
- 存储/网络失败保留当前图；恢复布局失败不阻止公式运行。
- 常量必须有限；整数窗口及范围沿用注册表规则。节点类型不信任客户端。
- 切换指标、结果类型或应用外部公式前，未应用画布必须明确处理；不能把旧图带到另一个指标。
- 不调用 eval。纯图遍历/参数校验属于编排，不新增 Python 数值运算。

## 6. 验收清单

- 标量、单通道与多通道图往返；共享子表达式；同源双输入；减除顺序；隐式/显式可选参数。
- 环、重复 ID、缺参、孤立节点、非法变量/算子、NaN/Inf、固定窗口、根类型、版本、规模边界。
- 解析不编译；应用后的定义通过原 NJIT 与 Excel 路径。
- 布局独立版本、指纹校验、冲突、不影响指标修订。
- 图未应用时阻止旧公式保存/计算；模式切换与迟到响应保护。
- 删除/撤销/重做、上游下拉、资源搜索/拖放、全屏/Escape、桌面和窄屏。
- 指标中心原回归、历史情景和 ETL 公共画布回归、生产构建、浏览器操作验收。

实施结果和实际执行的测试记录另见同目录验收报告；未执行项不得记为通过。
