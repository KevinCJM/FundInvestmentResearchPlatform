# 提交前修复与回归验收

日期：2026-09-11。分支：`codex/frontend-investment-process-framework`。

## 状态与范围

本轮发现的问题已修正；代码自审核及完整离线回归通过。提交范围和最终 Git tree 另经提交前门禁核对，未放宽数据、PIT、发布或 NJIT 约束。

本轮承接本地未提交代码审核，修正交付阻断，不重写业务算法。交付范围包含原 441 个候选路径，以及 `.gitignore`、两个旧后端测试文件、因子研究 E2E 和本验收文档，共 446 个路径；包括旧 TAA 前端服务的删除。原始 11 个暂存新增文件不是独立可构建单元，已按完整依赖重新纳入暂存。

远端的 `c3cbc40` 仅新增 11 张图片，已快进整合；未合并 `main`，不强推。共享计算、历史情景、PIT、ETF 择时、风险模型、配置研究、下载 ETL 及入口存在交叉依赖，本次按完整集成版本验收和交付，不机械分目录制造缺依赖的中间提交。

## 已修正问题

- F01：补齐所有新领域模块、路由、前端服务、组件、夹具和测试，暂存当前只读/跨步长 NJIT 签名，不再提交过期的局部暂存快照。
- F02：`frontend/e2e/process-framework.spec.ts` 显式模拟只读 `POST /api/tactical-allocation/preflight`，断言预检发生，同时保留“没有触发计算或持久化写入”的检查；生产预检门禁不变。
- F03：移除路由清单中被忽略的 `design-qa.md` 稳定引用，不强制提交本地 QA 产物。
- F04：两个风险预算测试隔离 `app.DATA_DIR`；行情缺失保留测试隔离 `instrument_routes.DATA_DIR`、清除外部路径覆盖并断言传入目录。PIT 解析和 NJIT 断言仍使用真实实现，不访问正式 PIT 锁文件。
- F05（完整回归新增发现）：因子研究 E2E 仍直接点击隐藏的内层“因子研究证据”。当前配置流程将其放在默认折叠的“参考：因子证据”中，生产入口可访问。测试补齐先展开父入口的真实点击，保留发布和引用断言，不使用强制点击或增加超时掩盖问题。
- 提交卫生：忽略 `/.tmp_project_*/` 和 `**/.vitest/`；原有 1,743 个临时文件保留本地，没有删除。
- 路由覆盖：将 64 个未登记路径归入现有模块，区分源码、测试、夹具和领域文档；补充 TAA 对风险成果仓储的依赖及 PIT 全局设置/页签查看覆盖的边界，不将可编辑口径冒充历史版本数据认证。

## 自审核

- 新路由与启动预热注册完整；TAA 保留按进程检查的 readiness 门禁及固定 NJIT 签名。
- 共享滚动计算、历史情景时点传播、不可变发布记录和 TAA 仓储依赖未拆漏。
- `ClassNavResult` 生产调用方已同步；被替代的 `taaBacktest.ts` 和进程级最近一次血缘变量没有残留引用。
- 独立检出副本的本地 Python 导入检查未发现未跟踪且未交付的依赖；这不等同于动态导入、经济有效性或生产部署认证。
- 候选文本的私钥、常见访问令牌及高置信字面量凭据扫描没有命中；不构成完整安全认证。

## 验证结果

| 检查 | 结果 |
| --- | --- |
| 本轮两个旧后端测试文件 | 14 通过 |
| 修复后的流程 E2E，320/768/1440px | 24 通过 |
| 全量后端 pytest | 3,176 通过，0 失败、0 错误、0 跳过 |
| 全量前端 Vitest | 851 通过，118 个文件 |
| 全量浏览器矩阵 | 最终 253 通过、0 失败、0 flaky、5 项按设计跳过 |
| TypeScript / Vite 生产构建 / 国际化 | 通过 |
| Ruff F821/F822/F823/F811 / diff 空白检查 | 通过 |
| 446 路径最终路由覆盖与干净检出检查 | 445 covered、0 uncovered；1 个删除路径按删除处理；校验通过 |

后端使用推荐 Python 3.12；Numba/BLAS/OMP 单线程控制，真实计算路径保持启用。测试工作区、日志、构建输出和浏览器报告位于独立临时目录。后端测试加载离线网络 guard，只允许 loopback，并额外阻止正式数据目录写入；本机 ETL 模拟服务获准绑定临时端口。前端测试限制为两个 worker；浏览器使用独立 Vite 端口 4237，不重启常用服务。

运行入口及原始证据：`/private/tmp/fund-submit-fixes-20260911.4lipNC/verify.py`，其中 `--browser` 是首次完整浏览器矩阵；修复后的最终完整浏览器入口为该目录的 `browser-final/run.py`。正式命令分别为 `python -m pytest backend/tests -q`、`vitest run`、`playwright test`、`tsc --noEmit`、`npm run build`、`npm run i18n:check`；准确环境、临时参数、退出码和输出分别保存在该目录的 `*.status.json`、`*.log`、`backend.xml`、`frontend.json` 及 `browser-final/browser.json`。临时日志不纳入 Git，最终提交的 Git tree 是源码版本依据。

AI Hermes 按 `evolve_ai_routing.py`、`validate_ai_routing.py` 与 R02/R76/R85/R86 路由 smoke 检查；检出副本独立验证，不能只依赖开发者本地忽略文件。`check_submission.py` 检查暂存清单、工作文件字节、SHA-256、独立 Git 检出清单、路由覆盖与本地导入；源码指纹和暂存内容在提交前再次核对。

后端全量测试期间仅补充了 `frontend/e2e/factor-research.spec.ts` 的一步点击：`changed_during_checks.json` 明确记录该项，未掩盖指纹变化。初次并行验收包装器因此返回 1，但后端、Vitest 和静态检查子命令均返回 0；后端、前端生产源码和 Vitest 测试文件均未变。最终浏览器使用修正后的完整套件另行执行，返回 0，且前后源码指纹无变化。首轮浏览器的 250 通过、3 失败、5 跳过证据没有覆盖或删除；两个修复后 E2E 文件的三档宽度定向合并复测为 27/27 通过。

5 项跳过的构成：3 个屏宽下的真实因子工作区 live 用例要求显式启用；另 2 项是同一历史情景快照检查仅在桌面执行，响应式已由前一用例覆盖。没有为通过回归而新增 skip。Vite chunk 体积提醒和已有测试警告仍保留，不将其描述为构建错误或已完成的性能优化。

## 下载及业务边界

按 `tushare-fetcher` 复核下载约束；项目未提供本地接口 JSON 时使用技能目录，账户机制返回 10,000 分，不将积分视为独立接口授权。`fund_adj` 技能旧目录与项目已记录的新版门槛有差异，本轮没有依据旧值降低配置或修改下载实现。持仓冲突采集的两个模块哈希仍与 `TushareDownload.md` 最新已验证记录相符。

本轮只跑离线下载契约/恢复测试，没有触发真实采集、快照激活或研究发布，也没有重做/升级接口烟测。浏览器中需要真实工作区变更和发布的 live 用例不自动启用。代码验收不代表策略具备投资价值、历史 PIT 已认证、供应商持仓冲突已解决或系统已部署。
