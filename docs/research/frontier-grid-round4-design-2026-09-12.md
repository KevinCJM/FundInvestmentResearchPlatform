# 第四轮整改：恢复整条有效前沿的逐目标求解

日期：2026-09-12。依据根目录 `saa-taa-recheck-round4-2026-09-12.md`、当前代码和本轮实测。原审核报告不修改；此文件说明实现、数值边界和验收。

## 1. 纠正需求解释

原设计的 20／200 是前沿的目标网格数量，单点求解器的迭代预算是另一个参数。此前仅改善三个代表点，虽然修复了不恶化、Pareto 和代表点重选问题，但并不等价于恢复原始功能。不能将此前限定“三个代表点、不做整条前沿”当作本次需求的范围依据。

本次恢复：确定前沿区间 → 生成 N 个收益目标 → 对每个目标执行受约束最小风险优化 → 返回逐点真实结果 → 绘制目标顺序曲线。第四轮报告接受收益网格或风险网格；本实现选择收益网格。保留随机候选探索和可选的三个代表点局部精炼，它们各自仍有独立用途、真实调用和回归，不是保存两份新旧实现。

实际算法不是 SciPy SLSQP，不沿用该名称。波动风险使用固定签名 NJIT 主动集二次规划；非二次风险使用固定签名 NJIT 可行 BFGS-SQP。前者具有凸问题的数值 KKT 检查，后者只提供局部数值停止条件。接口、页面和证据都明确区分。

## 2. 范围与四问

| 问题 | 判断 |
| --- | --- |
| 是否属于当前需求？ | 是。F1 明确要求按 20／200 个目标求解整条前沿，而不是增加随机点或三个点的迭代次数。 |
| 原有实现是否足够？ | 不足。现有局部精炼只有三个起点，没有目标收益约束，也没有 N 次求解及逐点状态。 |
| 最小必要改变是什么？ | 新增目标网格编排、约束 QP/SQP 数值步骤；扩展现有前沿 API、页面参数和结果，不另起一套资产数据或策略回测。 |
| 不做什么？ | 不改 SAA 的 CMA 政策模型、TAA 信号、交易执行或行情下载；不引入请求期 SciPy/Python 回退，不以画图平滑冒充优化精度，不实现混合整数权重优化。 |

## 3. 页面操作与草稿

“长期配置目标与研究区间”下，生成按钮附近直接显示 **按目标网格加密整条前沿**。开启后：

- **前沿目标点数**：整数 2–200，默认 20；控制 N 个目标及最多 N 个网格优化解。
- **单点最大迭代次数**：整数 1–1000，默认 300；每个目标和端点独立使用这个求解预算。
- 旧 `refineCount` 仍只属于可选局部精炼；新草稿独立保存 `frontierGrid.point_count/max_iterations`。不根据相同数字猜测旧字段语义，也不将旧局部迭代数自动当作网格密度。

随机采样、散点取整及可选局部精炼保留在高级区，并说明与整条前沿的区别。原采样取整后还会修复资金及组边界，因此不是严格的整数格点算法。网格解采用连续权重；同时开启采样取整时，必须主动确认这一差异才能生成，服务端也校验确认字段。采用网格权重不会再强制取整。

结果摘要区分采样候选、局部精炼新增、网格新增、总候选和前沿数量。逐点明细保留所有 N 个目标的收益、风险、权重、状态、迭代次数；成功解才能采用。端点失败时直接提示预算或约束问题；未开始的目标不能显示为成功。宽表只在自身区域横向滚动。

图表保留采样散点、共同候选的非支配点和三个最终代表点。网格曲线用真实求解坐标按目标顺序连接，`smooth=false`、`connectNulls=false`、`notMerge`；失败或未进入最终 Pareto 集合的位置断开，不补造坐标。重复目标和重复解单独计数，不宣称重复点提升曲线密度。用户修改目标数、迭代预算、区间或约束后，旧结果清除；迟到请求不能回填旧图。

## 4. 唯一计算链与 API

```text
ClassAllocation + FrontierGridControls
→ POST /api/efficient-frontier
→ 原有分类净值读取、收益转换与随机探索
→ 可选三个代表点局部精炼
→ solve_frontier_grid_kernel
   → 收益向量、风险模型与线性约束（一次构建）
   → 最小风险端点 + 最大收益端点
   → N 个收益目标、N 次逐目标尝试
→ 成功的网格解加入同一候选集合
→ 唯一 Pareto 筛选 + 唯一最终代表点选择
→ API、目标明细、曲线和采用权重
```

新增请求是独立的 `frontier_grid`，不将它装进旧的 `refine.iterations`：

```json
{
  "frontier_grid": {
    "point_count": 200,
    "max_iterations": 300,
    "weight_domain": "continuous",
    "accept_continuous_weights": true
  }
}
```

不提供该字段时不运行目标网格，原有请求行为不变。可选 `target_start/target_end` 支持显式数值区间，两者必须一起提供且有序；默认由两个求解端点自动确定。请求拒绝多余字段、布尔型点数、小数点数、越界预算及不完整区间。计算校验失败返回 HTTP 400，Pydantic 格式错误返回 422；不升级为无说明的 500。

每个 `frontier_grid.points[i]` 保存 `target_index/target/status/iterations/value/weights/optimality_residual/constraint_violation/candidate_index/duplicate_of/on_frontier`。失败迭代可保留有限的实际诊断值，但 `candidate_index=null`，不进入可采用集合。

响应明确区分：

- `requested_points`：目标数量。
- `attempted_points`：逐目标处理数量，包含经已求解上界证明不可行而直接拒绝的目标。
- `solver_calls`：真正执行过至少一次目标优化迭代的数量；可行的 20／200 目标验收要求恰好 20／200。
- `successful_points/failed_points/unattempted_points`：三者合计等于请求点数。
- `infeasible_targets/duplicate_targets/duplicate_solutions/added_candidates`：不可行、重复和新增候选分别留痕。
- `endpoints`：两个端点的状态、迭代数、残差和收益；端点调用不冒充目标网格调用。

原 `refinement.items` 继续记录各局部分支的改善；`final_representatives` 的来源增加 `grid`。最终三个代表点始终从采样、局部精炼和成功网格解的同一个集合重选，不能把某个分支的结果冒称集合最优。

## 5. 数学与求解边界

现有收益口径均为权重的线性函数，记为 `mu' w`，包括当前内核定义的平均、年化平均、累计求和及 EWM。此处复用既有数值真相，不借网格功能修改收益定义。

对目标收益 `r_k` 求：

```math
min_w Risk(w)
s.t. 1'w = 1,
     lo_i <= w_i <= hi_i,
     group_lo_g <= sum_{i in g} w_i <= group_hi_g,
     mu'w >= r_k.
```

标准差、年化标准差及 EWM 波动可写成 `sqrt(w' Sigma w)`，等价于最小化方差。样本协方差复用统一 `covariance_2d`；EWM 使用现有风险递推的极化计算交叉项，避免维护另一套衰减语义。均值—方差验收逐点对照解析解和测试专用 SciPy 参考；SciPy 不进入生产调用链。

主动集 QP/LP 从真实可行初值出发，求 KKT 方向，遇到边界加入独立约束，符号不合格的乘子移出主动集。最大收益端点是 H=0 的线性规划，走到实际阻挡边界；不是用巨大风险厌恶参数近似 LP。方向方程的小对角正则不改变原始目标和梯度，成功判据仍按原目标的数值残差及可行性检查。

非二次风险复用原风险内核，以有限差分梯度、受约束 QP 子问题、可行线段搜索和阻尼 BFGS 更新进行局部 SQP。差分步长及外层局部步长容差为 `1e-6`；不能把非光滑 VaR/ES/下行风险/最大回撤的局部停止解释为全局解。每个内层 QP 最多 300 次、线搜索最多 30 次，响应披露这些预算。达到外层上限、线搜索失败或数值失败均保留状态。

数值契约：QP 方向容差 `1e-9`，停止时原目标归一化驻点残差检查不超过该容差的 10 倍；最终资金/单项/分组/目标约束最大违反量不得超过 `1e-7`。边界误差不通过放宽经济约束或修补权重掩盖。

默认目标从已求解最低风险组合的收益到已求解最大收益端点等分。端点未收敛时不宣称区间已确定，N 行全部记录 `range_unresolved`。超出已求解最大收益上界的显式目标标记 `infeasible_target`；数值初值失败不当成数学不可行证明。奇异协方差、平坦目标或严格边界可能产生重复解；这不是 N 个不同有效点的保证。

## 6. 算子颗粒度与内存

QP 的主动集、KKT 方向、阻挡约束和乘子移除共享迭代状态，属于联合约束耦合内核；拆成普通无状态单向节点会改变迭代语义。外部独立步骤为模型与约束构造、目标网格、逐点调度、候选合并、Pareto 筛选及展示；没有把整个配置过程注册为新的黑盒算子。

历史收益矩阵只在输入边界做一次必要的 dtype/布局转换，网格内部复用同一个数组。固定签名接受只读任意步长输入；测试验证 `np.shares_memory`、只读跨步收益和约束、无输入改写及请求签名零增长。允许分配 N×资产数的求解结果、资产级 BFGS/KKT 工作区和非二次风险的收益向量；不会为每个目标复制 T×资产数的完整历史。

所有新增生产数值步骤在启动预热阶段执行，dispatcher 禁止新增签名；版本 `2.4.0`、22/22 固定签名内核。错误不会转入 Python、object mode 或外部 SciPy 求解。此实现仍使用优化线性代数作为 NJIT 内部操作，不代表整个请求零分配。

补充探针：3/8/12 资产、重叠分组和半正定 Hessian 的30组QP/LP与测试专用SciPy参考均通过，最大目标值差约 `2.96e-13`。本机2400×12只读跨步收益视图，共享底层内存，三次运行的网格内核中位数：20目标约3.44毫秒、200目标约13.15毫秒，均全部成功；不含导入、预热、取数、采样或JSON封装，不是API耗时或跨机器性能保证。进程峰值RSS约314MB包含导入/预热/参考库，不是网格单次新增内存。探针前后固定签名数量无增长，原始输入未改写。证据为 `numeric-evidence.json`。

## 7. 本轮实际验收

| 范围 | 结果 |
| --- | --- |
| 新增网格专项 | `backend/tests/test_frontier_grid.py` 31 项，包括 20／200 解析解、逐点独立求解器、组约束、连续口径确认、失败/重复/预算/非法输入、真实 API、只读视图与签名 |
| 网格 + optimizer + analytics 定向回归 | 55 passed |
| SAA/TAA/策略/大类同范围 18 个后端文件 | 392 passed；不是全仓后端重跑 |
| 前端全量 | 124 个文件、918 passed |
| 既有第二、三轮审核侧复现 | 原测试未修改，4 passed |
| 隔离 API + Chrome | 桌面 1440、手机 390 共 6 passed；包含原两条业务流程和新增网格流程 |
| TypeScript / Vite build / design check / diff 空白检查 | 通过，无新增设计检查回归 |

新增浏览器流程从政策页可见入口进入历史实验，实际修改为 20 和 200，断言真实请求、成功数量、实际求解调用数、约束和候选计数；比较两次真实 canvas 图像不同，再以单点预算 1 触发真实端点未完成状态。未使用模拟前沿输出替代数值验收。测试初次失败是用立即读取的可见性跳过了尚在加载的选择按钮，已改成等待真实按钮并点击后完整复跑；未放宽业务断言。

全仓515个Python源码的AST解析通过；CodeGraph已更新为965文件、19719节点、67197条边。Hermes结构校验通过，但路径覆盖明确识别出尚未Git跟踪的新QP文件、网格测试、前端组件和浏览器测试未登记，不能宣称路由覆盖完整；按项目稳定引用规则，纳入提交范围时应同步登记，不为通过检查伪装成诊断文件或改变忽略规则。

日志在 `.pytest_cache/saa-taa-round4-implementation/`，浏览器截图仍在 `.pytest_cache/top-down-allocation/browser/`。测试使用离线合成/临时 Parquet，无正式持仓、数据写入、生产服务重启、提交或推送。本轮不是交易可用性、投资有效性或全部生产 worker 的认证。

## 8. 方法参考

- SciPy 官方 SLSQP 文档：`maxiter` 是单次优化迭代预算，不是外层前沿目标数量。https://docs.scipy.org/doc/scipy/reference/optimize.minimize-slsqp.html
- Stanford / CVX Group 组合优化说明：收益、方差及权重约束构成的组合优化与风险收益前沿。https://www.cvxgrp.org/cvx_short_course/docs/applications/notebooks/portfolio_optimization.html

上述资料用于核对方法语义，不表示本项目复刻了 SciPy SLSQP。实际执行算法和限制以本文件及测试、源码为准。
