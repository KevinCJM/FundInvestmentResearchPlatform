# 语言与术语中心：现状分析与详细设计

日期：2026-09-07。状态：语言矩阵与动态语言列已完成本地开发和专项验收；旧页面全站翻译迁移未完成。当前实现以以下“矩阵模式实施说明”和《i18n_矩阵模式验收报告.md》为准，后文保留早期分析及设计背景。

## 矩阵模式实施说明（优先于早期单语言表格方案）

- 设置页保留系统只读表、业务可编辑表、业务修订历史。前两表统一为代码首列、语言横列，不再以中文标题和“默认／自定义”作为三个主要列。
- 全部分类显示完整键，如 `axes.asset`；筛选某命名空间时显示其内部代码，如 `asset`。底层始终按完整键加语言代码定位，翻译文本永远不是主键。单元格详情提供完整键和默认译文。
- 第一列和表头冻结。支持双击、Enter/F2 编辑，方向键定位，Shift 扩展区域，TSV 矩形复制粘贴；逐格或整块编辑只形成草稿，显式批量保存才发布。单次最多 1000 个变更，粘贴越界或存在非法单元格时整块拒绝。
- 表头“＋ 添加语言”和“管理语言列”接入动态注册表，最多 24 种语言；允许规范化后的语言—文字—地区代码子集，如 `ja-JP`、`zh-Hant-TW`，不支持扩展或私用标签。内置中文和英文配置受保护；新增语言可以改显示名、排序、启停，不能删除已登记的语言代码，停用保留译文。
- 语言回退只指向内置中文或英文，禁止跨语言继承用户的自定义术语。新语言的系统表显示“未提供系统语言包”；业务单元格没有实际译文时标记“未翻译”，另行提示运行时回退文本，不把回退文本计作翻译完成。
- 新增 `GET /api/i18n/matrix` 和 `PUT /api/i18n/languages`；原有 catalog、bundle、编辑、导入、历史接口保留。语言列表和默认语言共享 preferences 修订；业务译文有独立修订。写入统一按 preferences → business 顺序加锁。旧版语言偏好按原值兼容迁移。
- 新语言运行时会进入页面和顶部语言选择器；本地语言列表缓存支持已登记语言重载。语言切换只影响展示，不修改公式、用户备注或计算定义指纹。当前没有新增数值内核。
- 本次修复了窄屏固定列遮住译文、未提交编辑丢失、重置自定义语言时缺少回退预览、以及顶部仍固定双语言选项的问题。专项测试结果和项目级保留问题见验收报告。


## 本次实施约束（优先于以下早期方案）

- 设置页明确分为“系统翻译（内置只读）”“业务翻译（可自定义）”“修改历史”，不能靠前端禁用按钮保护系统词条。
- 系统目录 `locales/system.json` 与 `locales/navigation.json` 只随代码发布，不提供运行时修改接口；业务默认目录 `locales/business.json` 与领域注册表关联，运行时只写独立 `i18n_business_overrides.json`。工作区默认语言单独写 `i18n_preferences.json`。
- 翻译引擎使用 i18next / react-i18next，固定 system / business 命名空间，不允许跨命名空间回退；浏览器偏好与工作区默认语言分开。
- 普通编辑、导入、恢复历史全部经过同一后端白名单、占位符、长度、文本安全及乐观锁验证。系统词条无法通过伪造请求改写。
- 导入先验证并查看差异，再以绑定内容和版本的确认摘要应用；导出只含业务覆盖。恢复历史创建新修订，不倒退修订号。
- 全站可接入统一 t/term/Intl 边界；菜单全部接入，业务标签以稳定注册标识关联。未迁移的旧页面和缺失译文明确统计，不通过 DOM 替换或翻译输入值伪造覆盖率。


## 1. 代码现状与问题

本轮检查 `frontend/package.json`、前端源目录、设置路由和指标元数据，未发现统一 i18n Provider、语言词条注册及语言切换设置。当前不是完全没有中文映射，而是映射分散：

| 位置 | 当前职责与问题 |
|---|---|
| `frontend/src/components/Header.tsx` | 导航名称直接写在组件中 |
| `frontend/src/app/processRegistry.ts` | 阶段与设置项各自维护中文标签，和导航存在重复 |
| `backend/custom_indicators/typed_service.py` | `OPERATOR_LABELS`、`PARAMETER_LABELS` 维护算子名称与说明 |
| `frontend/src/utils/indicatorDiagnostics.ts` | 错误码中文标题以及技术文本替换 |
| `frontend/src/components/indicator-graph/indicatorGraphPresentation.ts` | 画布变量、类型、参数和维度的中文展示；部分通过正则处理后端描述 |
| `frontend/src/pages/IndicatorStudio.tsx` | 仍存在页面文案、类型展示和兜底提示 |

这些局部转换能修复当前页面，但不是可维护的全站国际化体系。例如现有 `hasChinese` 判断会把合法英文标签误判为“名称待补充”；不能在这套逻辑上直接增加英文开关。字符串替换也无法可靠表达语法、单复数和参数占位，更不能改写 LaTeX 或用户备注。

## 2. 核心边界：三类内容分开管理

1. **界面翻译**：按钮、菜单、提示、错误说明，使用稳定词条键。
2. **业务术语**：变量、算子、指标类型、维度、单位名称、数据字段含义，在领域注册表中绑定术语键；用户可以覆盖展示名称。
3. **用户内容**：自定义指标名称、步骤备注、产品名称、上传文件名，不自动翻译或覆盖。

计算标识保持不变，例如 `returns`、`periods_per_year`、`rolling_mean`、`scalar`、节点 ID 与通道 ID。修改“年化因子”为“每年观察期数”只改显示，不改变年化算法、数据口径或历史版本。

`scalar` 还需结合 dtype 区分“单个数值”和“单个判断值”；时间—资产矩阵与资产—时间矩阵必须保留维度顺序。翻译不能弱化计算类型契约。

## 3. 设置页面

建议位置：**设置 → 语言与术语**，计划路由 `/settings/language-terminology`。不能在只有部分页面完成迁移时宣称“全站英文”。

### 界面语言

显示当前界面语言、可用语言、已接入模块及覆盖率、缺失词条回退提示。第一阶段中文和英文；不通过切换语言修改交易日历、时区、币种、无风险利率或数字计算精度。

当前以工作区共享为主，尚不能假设存在可用的个人账号体系：界面语言先按当前浏览器保存。工作区默认语言和共享术语配置由设置页单独管理。后续接账号体系时再迁移个人偏好。

### 自定义术语

搜索与筛选区提供“模块、术语分类、语言、是否已覆盖”；表格展示默认名称、自定义名称、业务说明、使用位置及恢复默认。内部 key 收进技术详情，不迫使用户记代码。

示例：

| 内部标识 | 默认中文 | 用户覆盖示例 | 英文译名 |
|---|---|---|---|
| `periods_per_year` | 年化因子 | 每年观察期数 | Periods per year |
| `returns` | 复权净值普通收益率 | 复权净值收益率 | Adjusted NAV simple returns |
| `rolling_mean.window` | 窗口期数 | 滚动窗口 | Window length |

编辑后可预览节点、下拉选项与错误提示中的实际效果；保存共享配置前显示受影响的模块和条目数。恢复默认以删除覆盖项实现，不把默认值再复制进配置。

### 翻译与版本管理

支持按模块查看词条、待翻译项、缺失占位符、修改历史、预览差异、发布与回滚。导入必须先预校验，再让用户确认差异；不得导入后直接覆盖全部有效词条。

术语改名与翻译管理分开：普通使用者重点维护术语，开发维护者管理含占位符的完整文案。接入权限系统后限制共享发布权限；现阶段不得假称已有角色隔离。

## 4. 词条与覆盖模型

### 稳定键

- `navigation.settings`
- `indicator.graph.actions.validate`
- `indicator.graph.nodeKinds.constant`
- `domain.variables.returns.label`
- `domain.variables.returns.description`
- `domain.operators.rolling_mean.label`
- `domain.operators.rolling_mean.parameters.window.label`
- `domain.valueTypes.scalar.numeric`
- `domain.axes.time`
- `errors.GRAPH_CYCLE.message`

不能用当前中文原文作主键，也不能因为两个地方都是“输入”就合并成同一个领域术语。字面相同不代表业务含义相同。

### 默认词条

维护在版本控制中的 `locales/{locale}/{namespace}.json`。词条注册描述包含 key、模块、适用对象、默认语言、占位符、是否允许术语覆盖、字数上限和使用位置。注册表负责绑定 key；翻译目录负责各语言的文字，避免前后端各自维护完整默认文案。

### 工作区覆盖

独立持久化：`schema_version`、`revision`、`default_locale`、`overrides[locale][key]`、变更时间、历史版本。沿用项目的原子写入和独立乐观锁，避免两个人同时改术语造成覆盖。

同一目标语言内先取工作区覆盖，再取系统默认；不存在时按显式 fallback 语言规则回退。某语言的自定义中文不能覆盖另一个语言的默认英文。无法解析的 key 提供可定位的中文兜底及诊断，不能在业务页面直接裸露原始代码标识。

首次不加入“个人术语覆盖”，避免同事在同一套研究结果里看到不一致术语；将来确有需求，再在个人语言偏好之外单独设计。

## 5. 前后端职责

### 前端

新增 `frontend/src/i18n/`，包含加载、语言偏好、命名空间、占位符检查、格式化和订阅更新；业务组件仅调用统一 `t(key, parameters)` 与 `term(entityType, id, field)`。

采用可替换的成熟国际化适配层，不自行用字符串替换重建语言引擎。具体依赖与版本在开发时核定。旧组件允许逐步接入，不通过扫描 DOM 或全局替换已经渲染的中文实现“翻译”。

标签在渲染时解析，不能在模块 import 阶段固化；否则切换语言后导航与列名不会更新。图节点、端口、下拉选项、图例、工具栏、小地图辅助文本、资源搜索与图表提示统一接入。搜索同时索引系统默认名、自定义名、译名和内部代码，保留熟练用户按代码查找能力。

数字/日期采用统一格式化边界：接口仍使用 ISO 日期和原始数值，不把本地化百分比字符串传回计算接口。导出文件的语言与术语版本需显式记录；更换界面语言不改历史文件。

### 后端

领域元数据逐步提供 `label_key`、`description_key`，保留当前 `label` 等兼容字段供旧前端使用。异常逐步改为结构化 `code + message_key + message_params + field + editor_node_id + expected_type + actual_type`，而非要求前端从一整段中文中猜参数。

错误数据只带白名单业务参数，不传堆栈、原始 SQL、凭据或其他内部详情。旧错误仍可在兼容适配器处理，新代码不得再依赖正则翻译。

国际化配置不进入指标定义哈希、计算计划键、数据快照或 NJIT 签名；不触发数值缓存失效。它仅影响呈现缓存，使用独立语言配置修订号刷新。

### 计划接口

| 接口 | 用途 |
|---|---|
| `GET /api/i18n/catalog` | 返回已登记词条、模块、允许覆盖字段和语言覆盖率 |
| `GET /api/i18n/settings` | 默认语言、当前工作区配置修订 |
| `GET /api/i18n/bundle?locale=...&namespace=...` | 合并后的展示词条，提供独立缓存版本 |
| `PATCH /api/i18n/overrides` | expected_revision 加词条修改集；冲突返回 409 |
| `POST /api/i18n/import/validate` | 对文件作有界预校验并返回差异，不保存 |
| `POST /api/i18n/import/apply` | 对已确认差异做版本检查后应用 |
| `GET /api/i18n/history`、`POST /api/i18n/restore` | 查看与恢复历史展示配置 |

导入校验包括文件/条目数/文本长度、未知 key、重复项、locale、占位符完整性、保留对象键和禁止 HTML/脚本。覆盖值使用文本渲染；不得将导入内容放入 `dangerouslySetInnerHTML`，更不能作为 LaTeX 或代码执行。网络失败使用随包提供的默认词条，不让研究页面因翻译服务故障而不可用。

## 6. 迁移计划和验收

第一阶段：建立共享词条注册、中文默认包、设置页及工作区术语覆盖；迁移指标中心与公共导航。即使仅用中文，也立即解决同一名词散落多处的问题。

第二阶段：补齐已接入模块的英文包、语言切换及模块覆盖率；迁移历史情景、数据源、因子研究和评价方案。未迁移的模块明确标注仍使用中文。

第三阶段：覆盖全系统页面、图表、表格、弹窗、错误与导出，完成全站多语言验收。阶段完成以自动扫描和真实浏览器覆盖为准，不以“有语言切换按钮”为准。

验收必须覆盖：覆盖后跨页面一致、恢复默认、并发 409、刷新保留语言、词条缺失回退、占位符错误、长文本与窄屏布局、辅助功能标签、用户名称不被改写；修改术语前后规范公式、定义哈希、结果与 NJIT 执行路径完全一致。

## 7. 本轮交付边界

画布节点外观、常量显式节点与操作栏精简已经进入本地开发；本文件是全站语言与术语模块的后续实施设计。未新增空壳语言设置页面，未安装国际化依赖，也未把现有页面描述为已经完成多语言迁移。
