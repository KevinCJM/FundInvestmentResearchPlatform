# 情景算子颗粒度治理与组合化设计

## 目标与边界

将 AGENTS.md 中的“最小独立计算语义”原则落实为真实注册信息、可编辑的组合图和唯一数值实现，而不是只改名称或隐藏大节点。当前工作区已有数据下载、PIT 和节点预览等未提交改动，本需求不覆盖或回退这些改动。

首批等价拆分对象：三状态阈值、双指标四象限、PS 峰谷定界。阈值与四象限补齐可复用的条件/状态类型桥接；PS 拆出峰谷定位、联合约束筛选、完整区间、阶段方向、区间收益、边界连线与小波段震荡合并。其他算法按同一规范审计；没有等价证据的算法不自动替换或伪装成已完成拆分。

## 三个视角的共同决策

### 产品经理

用户选择完整方案时应能快速得到可运行结构，研究某一步时应能替换该步而不改其他步骤。完整业务方案归类为“组合模板”，不能和基础算子混在同一目录层级。保留有意义的耦合内核，不把画布做成逐语句编程器。

### 架构师

注册表决定颗粒度类别、理由和可展开能力；服务端负责展开并重接所有输入、输出、下游引用和显式边。展开是纯定义转换，不取数、不执行、不保存、不准备计划。计算仍经过现有 infer → prepare → preview/run → publish。原始保存版本、快照和发布门禁不改变。

原有 PS 包装函数委托拆出的唯一内核；删除被提取的重复检测和筛选代码。旧入口只服务真实的已保存定义、历史模板和现有 API/测试契约，不留备份实现。条件比較复用指标中心的数值比较原语，领域层只补缺失传播与条件类型边界。

### 人类用户

资源库清楚显示“基础算子 / 组合模板 / 耦合内核”，组合卡片写“添加并展开”，说明将加入哪些步骤。普通节点保持“添加”。已保存的大节点提供显式“展开为计算步骤”动作，单次可撤销；不在读入时自动迁移。实时模式下事后模板仍不可添加。异步展开返回时校验草稿身份，不能覆盖随后编辑。

## 颗粒度审计

| 对象 | 职责与中间结果 | 处理 |
|---|---|---|
| 三状态阈值 | 比较与条件选态可单独使用；score 是原输入，编码与时点是独立输出适配 | 组合模板 |
| 双指标四象限 | 两组阈值比较与条件选态可复用 | 组合模板 |
| PS 整体 | 定位、筛选、分段、统计与震荡合并可单独替换 | 组合模板 |
| PS 联合约束筛选 | 删除一个峰谷会改变相邻阶段、完整周期及首尾约束，必须重复检查至不再删除 | 保留耦合内核；每轮至少删除一个拐点，保证终止 |
| 小波段震荡合并 | 从最早起点寻找最长完整合格波段，幅度、方向结构和路径效率相互约束 | 保留独立耦合内核；返回合并区间及同一次选择的证据 |
| 滞回、连续确认 | 需要维护活动状态、候选计数及保持期 | 保留状态递推内核；不能当无状态过滤串联 |
| HMM / Markov / GMM | 拟合与推断为完整模型求解；特征准备和业务标签映射属于外围 | 保留模型核心，不拆为用户操作的 EM 循环 |
| 趋势特征包、滤波状态方案、结构突变 | 仍有可独立替换的特征、条件和递推边界 | 保留审计标记；仅在对应等价回归完成后开放展开，不能假称已原子化 |

多输出不是重型算子的判据：峰谷筛选返回位置与相应价格属于一个事件检测问题；把无关指标放在一起则不成立。

## 数值与类型契约

1. 新条件端口采用独立名义类型 `condition<int64>`：1 真、0 假、-1 缺失。不能连接到数值或状态端口。逻辑组合严格传播缺失；缺失不是假，更不是震荡。
2. 条件选态只接条件与可选状态分支，缺失条件输出 -1；只使用被选中的分支。默认状态码由参数给定，校验范围 -1 至当前状态数减一。
3. 原三状态阈值保留 `>= upper`、`<= lower`；区间阈值保留严格 `>`、`<`。不统一等号口径。
4. 确定性状态编码输出是 one-hot 隶属编码，有效项指示值 1 不代表预测正确率或校准概率。UI 标签应说明用途，不新增统计解释。
5. PS 阶段方向使用独立 `phase_codes<int64>`（0 上行、1 下行、-1 未完成），不能当成三态市场标签。震荡合并输出才映射至定义中的市场状态。
6. 保留所有原始缺失、非正价格断点、平台最早极值、半开区间 `[start,end)`、完整终点价格与未完成尾部。区间起止必须来自同一分段节点，不允许混接。
7. 阶段数以输入观测间隔计，不替换为自然日。大幅变动例外只放宽最短阶段，不放宽完整周期。
8. 所有新节点在输出可得时点上合并全部输入依赖；PS 的全样本知识范围随下游传播，不能因末端变成普通比较而获得实时资格。

## 拆分结构

### 三状态阈值

输入 → 与上界比较（含等号） → 条件选态（牛 / 下游结果）

输入 → 与下界比较（含等号） → 条件选态（熊 / 震荡）

score 引用原输入；需要旧 probability/confidence/recognition/reason 输出时接独立编码与时点节点。最终图只执行实际依赖，不为每个分类默认生成无用多输出。

### 四象限

增长 ≥ 增长界线、通胀 ≥ 通胀界线 → 两个通胀分支 → 按增长条件选态。保持旧 0、1、2、3 的象限顺序。原 score 仍为增长减通胀。

### PS

原始价格 → 峰谷定位 → PS 联合约束筛选 → 相邻峰谷分段。

同一分段 → 阶段方向 / 区间涨跌幅 / 峰谷连线。

阶段方向 + 原始价格 + 同一分段 → 小波段震荡合并 → 最终状态及合并证据。

提取后的旧 PS 包装函数也调用上述内核，避免两套数学逻辑。现有独立日频模板不冒充 PS 等价模板，因为它本来就没有 PS 周期筛选和多波段震荡规则。

## 前后端实现约束

- 展开接口只接受一个注册且有展开契约的节点。校验节点版本、参数、状态数和展开后节点/边预算；允许尚未连接完成的草稿，但不允许把已存在的非法引用当作成功迁移。
- 所有输出都有明确映射，包含非主输出、显式边、exposed nodes 和命名输出。保留其他节点、图表元数据、数据源绑定及布局；新节点以原节点位置为锚排列。
- 不自动覆盖最终输出或用户的状态名称/颜色，不隐式增加数据源，不持久化任何转换结果。
- 组合目录给出步骤概览；尚不支持展开的复合节点显示真实状态和原因，不伪标为基础算子。
- 新增普通数值节点使用固定 dtype、连续数组和固定签名 NJIT；导入/启动完成预热后禁用新签名编译。纯图转换保持 Python 编排，不进入数值路径。
- 服务实际执行路径与审计中的 kernel_id 同步注册。性能验证记录绝对耗时和额外中间数组成本，不在没有测量时声称加速。

## 验收

- 三状态：上下界等号、NaN/Inf、空输入、不同阈值；组合与旧入口逐项相等。
- 四象限：所有组合及恰等边界、缺失；状态/score/编码与旧入口一致。
- PS：现有独立参考测试全部继续执行；随机样本、分段缺失、平台、短阶段例外、短周期、双/三态与震荡开关；各端口等价。
- 图转换：输入未连接、任意节点 ID、全部输出、下游连接、显式边、布局、状态信息、版本与预算；转换不写仓库且不修改源对象。
- 因果性：事后图实时拒绝、全样本可得时间传播、未知尾段不补齐；正式发布门禁不放宽。
- 前端：目录分类、添加并展开、旧节点显式展开、异步草稿竞争、撤销、不可用和实时禁用、画布/引导/公式往返。
- 执行：实际进入已预热的 NJIT，零请求时编译、零 Python fallback；运行相关后端回归、前端测试及生产构建。

## 完成记录

2026-09-09 当前进度：

- AGENTS.md 已写入颗粒度治理原则。条件、状态选择、PS 联合筛选、波段方向与边界连线已接入固定签名 NJIT；旧入口委托共享内核。
- 纯定义展开接口 `/api/historical-regimes/authoring/expand` 已接入。展开保留全部输出映射、下游连线、显式边、参数及状态定义；未完成输入可以保留草稿，真实类型或轴冲突仍拒绝。
- 节点资源库区分基础算子、组合模板及耦合内核。三状态、四象限、PS 支持添加并展开；已有节点支持显式展开。画布和引导模式共用交互，展开只产生一次可撤销编辑，过期响应不覆盖草稿。状态参数显示业务名称并提交整数编码。
- 推荐列表新增独立 ID 的分步模板；原模板精确 ID 与内容哈希不变，历史定义和发布快照不自动迁移。
- 核心颗粒度与分段测试 75 项通过，逐输出校验数值、日期、可得时间、PS 缺失与尾部、固定 NJIT 签名及零 Python 回退。
- 情景、图谱、峰谷、趋势、压力和研究序列相关后端回归：432 项通过，1 项全应用启动测试单独未完成。该项在指标中心 `warm_numba_plans → compile_numba_batch_plan` 启动预热中超过执行时限；没有放宽 readiness 或跳过生产预热。
- 全量前端测试最终 101 个测试文件、683 项全部通过。`ClassAllocation.test.tsx` 的合规发布夹具补齐 `uses_future_data=false` 和 `repaints=false`，与已有严格门禁一致；没有修改业务页面或放松发布门禁。
- TypeScript `tsc --noEmit` 与最终 Vite 生产构建通过。构建仍提示已有大包体积警告，没有借本需求升级依赖或修改打包架构。
- 新增浏览器验收使用真实后端注册表与展开函数、隔离的离线固定样本，320/768/1440 三种宽度全部通过：已有组合展开、添加并展开、业务状态下拉、全部连线和最终输出保留、单次撤销、无保存副作用。另有原工作台浏览器回归 4 项通过、2 项按既有响应式分工跳过。浏览器发现引导模式缺少撤销入口，已将撤销/重做移至三种编辑方式共享区域并复验。
- 性能脚本 `backend/scripts/benchmark_regime_granularity.py`：2 万观测、固定随机种子、预热后 9 次中位数。三状态包装/展开分别 0.2166/0.2717 ms，四象限 0.3203/0.2944 ms，PS 1.4496/1.5542 ms；所有输出逐项完全一致，无新签名编译和 Python 回退。仅测数值节点执行，不包含 I/O、结果 JSON、全服务冷启动；不是整条 API 的延迟承诺。唯一保留 NumPy buffer 分别由 1.60/1.44/2.08 MB 增至 2.08/2.08/2.72 MB，不是进程峰值内存。分步可调试性有小幅中间数组成本，不能宣称全面加速。
- `docs/repo_map.json` 已同步职责、入口、相关测试和回归命令。本次新增路径覆盖检查通过，R76 路由解析通过，`git diff --check` 及暂存差异检查通过。全仓库路由校验仍被数据下载任务的三个未追踪文件阻塞：`index_checkpoints.py`、`test_index_checkpoints.py`、`test_constituent_download.py`；这些不属于本需求，未擅自纳入提交或修改。
- 未完成的运行验收仅为上述全应用冷启动测试；全仓库路由校验也不能宣称通过。核心情景回归、前端全量、构建、类型检查和新增浏览器验收已有实际通过证据。
- 趋势特征包、滤波状态方案、结构突变等仍按目录标记整体执行，没有宣称已等价展开。本需求新增文件已纳入 Git 暂存追踪，未创建 commit 或推送；已有工作区修改保持未提交，其他任务的改动未覆盖。
