# 因子研究中心：风格暴露与收益贡献详细设计

日期：2026-09-06。范围：在现有因子收益率模块补全收益回归 → 暴露 → 逐日贡献 → 多期贡献对账。状态：前后端本地开发与相关功能回归完成；项目级检查限制见文末。

## 1. 范围与现状

现有 `FactorResearchService.run_attribution` 支持 RBSA / FF3 / 通用回归，返回暴露、截距、解释度和残差波动，没有逐期贡献或累计对账。复用因子数据集、市场/币种校验、净值读取、原子运行存储、固定签名 NJIT 和既有拟合器，不重建因子库。

本次不增加 PBSA、Brinson、基金底层持仓穿透、交易或股票数据下载。因子归因是模型分解，不是实际交易损益台账，残差也不是基金经理能力的直接证明。

## 2. 核心计算契约

### 2.1 暴露与每日贡献

总收益模型：r[t] = Σ beta[t,k] f[t,k] + alpha[t] + epsilon[t]。
超额收益模型：r[t] = RF[t] + Σ beta[t,k] f[t,k] + alpha[t] + epsilon[t]。

同一次模型估计的 beta 同时用于暴露展示与贡献计算。每个因子贡献 c[t,k] = beta[t,k] f[t,k]。截距、残差分别列示；RF 只在超额收益模型单列，总收益模型不虚构无风险收益数据。RBSA 的指数代理与其模型约束保持不变：系数非负、和为1。FF3/通用回归系数不解释为真实持仓比例。

每日独立输出实际收益、因子贡献、RF（适用时）、截距、残差、贡献合计、对账误差与状态。误差检查只证明代数闭合；模型解释力仍需看样本外 R²、残差与覆盖，不能因为对账正确就称模型有效。

### 2.2 固定与滚动模式

- `exposure_mode=fixed`（默认）：沿用样本内一次拟合，同一 beta 解释全部日期。样本内是事后描述，不能作为当时已知暴露；样本外系数固定。
- `exposure_mode=rolling`：从研究起始日起，积累完整 `rolling_window` 个交易日位置后，使用 [t-window,t) 拟合，绝不读取 t 或之后的收益。每隔 `refit_step` 个交易日重新估计，间隔内沿用最近一次计划估计；拟合失败不偷偷保留旧成功模型。`min_observations` 检查完整有效配对数；缺失日期保留，不把缺失压缩成更长实际窗口。
- 默认滚动窗口126、最少60个完整配对、每21交易日重新估计，可配置。底线 max(30, 5×因子数)。基金收益的两个端点公告日期必须早于本次估计生效日，尚未公告的历史净值不能进入该次拟合。
- 记录估计窗口起止、有效样本数、训练 R²、失败原因和生效日。样本外滚动属于 walk-forward，会使用此前已经过去的样本外数据，不声称是冻结训练集。
- 当前因子数据集没有完整历史修订和发布时间档案，因此滚动结果仍为历史研究，不保证实时 PIT 可交易性。

### 2.3 多期贡献：期初财富加权串联

不能把每日贡献简单相加当成复利贡献，也不能分别连乘各因子再求和。对指定区间从财富1开始：

V[t-1] = Π(s<t)(1+r[s])
C[k] = Σ[t] V[t-1] c[t,k]
Σ[k] C[k] = Π[t](1+r[t]) - 1

这是明确的顺序财富加权分解，不称为 Carino/Brinson，也不要求因子腿可独立投资。截距、残差、RF 同样串联。每个样本内、样本外、月份、全区间分别从1重置；月份贡献不能直接相加当作全区间贡献。

累计曲线始终明确区间。任一实际收益缺失或不合法，完整区间总收益不可用；任一贡献日期不可计算，完整区间因子贡献和对账不可用，禁止跳过缺口假装完整结果。其后独立月份仍可正常计算。滚动预热期不纳入贡献评价，但明确展示剔除日数、实际评价起止。正常的真实零收益保留为0；缺失保持null。

## 3. API 与持久化

继续使用 POST `/api/factor-research/attributions`，新增可选参数 `exposure_mode`、`rolling_window`、`min_observations`、`refit_step`；老请求默认固定模式。既有结果字段保留，新增 `attribution`（版本1）结构：

- `mode`、`linking_method`、`components`（因子/无风险/截距/残差的类型化ID）、`evaluation_start`、`warmup_days`。
- `products[]`：按产品的 daily（含估计证据、暴露与贡献）、summaries（全区间/样本内/样本外/月度）、curves（按所选样本独立串联）。
- 每个 summary 包含起止、应有/有效日数、实际复利收益、贡献、贡献合计、对账误差、模型R²、残差年化波动和完整性状态。

新增 `attribution_kernels.py` 负责纯数值，`attribution.py` 负责编排与序列化；主 service 保留数据和模型校验。原始输入、公告日、暴露路径、逐日贡献均与运行一起冻结为 NPZ，旧运行不修改、不自动补算。

响应按200万结果单元限制；滚动总拟合次数有限额，超过则拒绝并提示减少产品/区间或降低重估频率。所有新增 kernel 显式固定签名，启动预热纳入 readiness 和实际执行证明，禁止请求期编译或 Python 数值回退。

## 4. 前端

入口不变：因子收益率 → 收益归因，内容升级为风格与收益贡献研究。配置区增加固定/滚动选择，滚动参数折叠显示，明确收益口径和日期含义。

结果独立组件 `AttributionResults`，避免继续堆叠工作台代码。选择产品和评价区间后展示：

1. 完整性、实际累计收益、贡献合计、对账误差、模型解释度。
2. 风格暴露曲线；固定模式明确训练期暴露为事后估计。
3. 各因子与RF/截距/残差贡献柱形、累计贡献与实际收益对照曲线。
4. 月度对账表、分页每日明细（估计窗口与暴露可核对）；导出当前产品的逐日贡献 CSV 和运行 JSON。

贡献用“百分点”，暴露用系数，不能把系数标成持仓占比。默认查看样本外。图表局部缩放，表格局部横滚，320/768/1440宽度不得撑破页面。旧结果无 `attribution` 时显示原回归表及“旧运行未保存贡献，请重新运行”，禁止由前端凭摘要反造。

## 5. 验收

- 独立参考验证 RF/截距/残差完整性、负贡献、零收益、并列/常数/秩亏与 NaN/Inf。
- 手算两日复利贡献闭合；反例证明简单相加错误；全期/月度重置和缺口传播。
- 固定模式与旧拟合结果一致；滚动仅使用历史配对，延迟公告与未来数据扰动不改变此前暴露，失败窗口不保留旧系数。
- 真实 NJIT 调用路径、签名数量不增、冷服务503、参数422、计算规模限额、存档不变性。
- 前端模型/模式切换、历史兼容、百分点格式、导出转义、失败状态、三种屏幕宽度端到端。

## 6. 方法依据

Sharpe (1992), Asset Allocation: Management Style and Performance Measurement，原作者全文：https://web.stanford.edu/~wfsharpe/art/sa/sa.htm 。其风格模型与滚动样本外思想用于本项目的暴露/收益分离；上述财富加权串联恒等式在本设计中直接给出并由独立算例测试，不将它冒称为某个商业模型。

## 7. 实施与验收记录

### 7.1 实现文件

- 后端 `backend/factor_research/attribution_kernels.py`：4个固定签名内核（收益公告可得日、固定/滚动暴露、每日贡献、财富加权串联），使用既有 `attribution_kernel` 拟合。
- `backend/factor_research/attribution.py`：输入规模与拟合次数限制、全期/样本内外/月度编排、证据与缺失状态序列化。
- `contracts.py` / `service.py`：兼容旧请求，新增模式与参数，接入启动预热、执行证明及不可变运行。没有另建因子库或新归因端点。
- 前端 `AttributionWorkbench.tsx` / `AttributionResults.tsx` / `attributionPresentation.ts` / `factorResearch.ts`：配置、图表、累计/逐月逐因子/逐日对账、CSV/JSON导出、旧记录只读降级。
- 新测试：`backend/tests/test_factor_attribution.py`、`frontend/src/components/factor-research/AttributionResults.test.tsx`、`frontend/e2e/factor-attribution.spec.ts`；界面夹具为 `frontend/src/test/factorAttributionFixtures.ts`。

### 7.2 实际执行结果

1. 后端相关回归 **73 passed**：
   `python -m pytest backend/tests/test_factor_research_numba.py backend/tests/test_factor_research_service.py backend/tests/test_factor_return_kernels.py backend/tests/test_factor_return_service.py backend/tests/test_factor_attribution.py backend/tests/test_product_pools.py -q`
2. 前端相关回归 **33 passed**：
   `npm run test --prefix frontend -- --run FactorResearchCenter.test.tsx FactorReturnCenter.test.tsx AttributionResults.test.tsx App.test.tsx ProductDetail.test.tsx --silent`
3. 浏览器离线端到端 **15 passed**，覆盖320/768/1440宽度、两种暴露模式、旧研究/收益构建链路、下载的CSV实际内容：
   `npm run test:e2e --prefix frontend -- factor-research.spec.ts factor-returns.spec.ts factor-attribution.spec.ts --workers=3`
4. `npm run build --prefix frontend`：通过。仍提示已有依赖浏览器数据较旧和大包体积。
5. 新增后端15项包含独立手算两日贡献、负/零收益、RF缺失、NaN/Inf、秩亏、冷服务、资源限制、公告延迟、未来扰动不改变此前暴露、存档不变性和实际NJIT调用。并非仅靠存在装饰器认定加速。

### 7.3 未通过的项目级检查

- `tsc --noEmit` 返回2：诊断出现在未修改的指标、产品比较、组合页面测试及 `ResearchDataLab.tsx` 等文件；本次新增文件无类型诊断。不能把生产构建通过说成全项目类型检查通过。
- 路由校验返回1：原因子中心目录、既有测试/配置/文档仍未Git跟踪。显式13项变更覆盖检查为9项覆盖、4项待登记（新后端测试、新浏览器测试、新界面夹具、本设计文档）。新的未跟踪路径尚未登记为稳定机器路由引用；现有路由的行为说明、关键词和隐藏契约已同步。
- 上述4条路径在提交前须进入版本控制并登记进 `docs/repo_map.json` 的相应文件/测试列表，届时更新最小回归命令并重跑演化/路由校验；不能仅靠静默放宽校验或添加空白豁免使其通过。

本次未执行Git提交、推送或服务部署。测试写入隔离目录/浏览器测试输出，没有生成正式研究发布、产品池候选或下载外部数据。现有其他模块的未提交业务改动保留。
