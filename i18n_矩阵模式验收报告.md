# i18n 矩阵模式：实施与验收报告

日期：2026-09-07。范围：延续语言与术语中心，将单语言表格改为代码首列的多语言矩阵，并接入动态语言注册。当前为本地工作区交付，未提交、推送或部署。

## 1. 实际交付

入口：设置 → 语言与术语。

系统翻译、业务翻译继续分表。系统表只读，业务表允许编辑；API 拒绝通过普通修改或导入覆盖系统词条。系统包来自 `locales/system.json`、`locales/navigation.json`，业务默认词条来自 `locales/business.json`。

表格首列显示稳定系统代码。在全部分类下显示 `axes.asset`，筛选“对齐维度”时显示 `asset`；中文“资产”、英文“Asset”分别位于各语言列。名称、说明等独立词条保留完整键的语义，不根据译文字面相同合并。只读代码可复制，不能修改。

语言列支持添加、调整顺序、编辑语言显示名、选择内置回退语言、启停。内置中文与英文受保护；已登记语言不提供删除或修改代码，停用后保留词条。最多 24 种语言。当前接受语言、可选文字、可选地区的规范代码子集，不接受私用/扩展标签。

单元格支持双击、Enter/F2 编辑，Enter 确认、Esc 取消，Tab 移动，方向键定位，Shift 扩展矩形选择；支持 TSV 区域复制粘贴和统一批量保存。单次最多 1000 项修改；越界、非法占位符、危险文本或不可编辑单元格会导致本次粘贴整体拒绝。手机保留横向表格和更窄的冻结代码列，不让整页横向溢出。

表格基础能力补齐为服务端全量搜索、筛选和排序：搜索同时覆盖完整系统键、短码、内置译文、自定义译文和回退译文；系统代码列以及每一种动态语言列都可点击表头切换升序/降序，排序发生在分页之前，缺失译文稳定排在有值项之后；提供“一键清除筛选”。因此翻页后的顺序仍保持一致，而不是只排序当前 50 行。

缺少实际译文显示“未翻译”，运行时回退文字单独提示。用户新增日语等语言列不会自动产生系统语言包：菜单使用所选内置回退语言，业务词条使用本语言译文，缺失时退回内置默认译文，不能串用其他语言的工作区自定义术语。顶部语言选择器同步列出已启用的动态语言。

## 2. 数据和接口

新增 `GET /api/i18n/matrix`：在同一配置快照中返回分页词条、全部语言列及各单元格实际值、默认值、覆盖值、来源和回退信息。首列短码只影响呈现，提交仍使用完整词条键。

新增 `PUT /api/i18n/languages`：以 preferences 修订号保存语言注册表。默认语言与语言列表共享该修订；业务译文单独使用业务修订号。统一使用 preferences → business 锁顺序；旧 preferences 文件兼容迁移，不改业务覆盖。

保留原 catalog、bundle、业务修改、JSON 导入导出、历史恢复接口。导入必须先预览再确认，确认摘要关联业务修订、目录版本和语言配置修订。语言配置变化后旧预览不可直接发布。

运行时数据分别写 `i18n_preferences.json`、`i18n_business_overrides.json`；没有为每种语言新增数据库字段。公式、节点/变量标识、用户名称/备注、计算缓存与指标修订不由译文改变。

## 3. 本轮自审核修复

- 冻结代码列在 320px 下过宽，拦截译文鼠标操作：改为明确列宽、窄屏 96px，并通过真实双击编辑复验。
- 单元格未确认或校验失败时可能丢失：编辑期间限制切换筛选/分页和发布，保留输入，支持 Enter/Esc；刷新/离开浏览器有草稿保护。
- 新语言已有覆盖时缺少默认回退数据，恢复默认预览错误：后端持续提供必要回退元数据，并测试重置后的显示和运行时一致。
- 顶部选择器仍固定中英文：改为从语言注册表渲染启用项。
- 翻译改名后按原名无法搜索：矩阵搜索同时检查完整键、内置名称和自定义译文。
- 旧 catalog 接口未使用自定义语言的回退设置：补齐，与 bundle 保持一致。
- 测试代理在页面重载/关闭时仍有未完成请求：测试显式等待页面请求完成并排空路由处理，不屏蔽实际 API 失败。
- 工作区新增指标结果设置组件引用未登记的 `scalarOutputs.*` 文案：按组件已有含义补入系统只读中英文包，未修改其业务逻辑。

## 4. 实际测试结果

| 范围 | 结果 |
|---|---|
| 后端 localization 原回归、矩阵/动态语言、指标图契约 | 70 项通过 |
| i18n runtime、Provider、矩阵解析/交互、设置页 | 39 项通过 |
| 真实本地 API 浏览器验收 | 15 项通过：320×800、768×1024、1440×1000 各 5 项 |
| `npm run i18n:check` | 通过；当前登记系统词条 504、业务词条 384 |
| `npm run build` | 通过；保留依赖数据陈旧与较大前端包警告 |
| 全项目 TypeScript | 未通过；该次最终检查 34 处错误，本轮 i18n/矩阵/设置页/Header 路径不在错误列表 |

浏览器测试使用隔离 FastAPI 服务与临时存储，未写正式工作区翻译；覆盖系统只读拦截、多语言改名、跨页面更新、语言切换后定义指纹不变、用户备注不变、导入差异/导出/历史恢复、离线回退、动态日语注册/重载/停用保留、冻结代码列，以及系统代码/语言列排序、跨语言搜索和清除筛选。已查看最终桌面和手机截图。矩形粘贴通过浏览器 ClipboardEvent 注入与 Excel 相同的 TSV 数据，不是操作真实 Excel 进程；本轮不提供 XLSX 文件导入。

### 项目级保留问题

19:15 的全量 Vitest 为 565 项中 563 通过、2 失败：大类配置历史情景选项测试失败；产品比较出现一次元素被移除的断言失败。独立重跑产品比较 4 项通过，大类配置仍失败。此后工作区指标中心出现多标量输出接入修改，相关 40 项测试有 2 项旧提示文本断言未匹配。没有为制造全绿而回退或改写这些并行开发中的业务代码；不能将本次专项通过表述为全项目回归通过。

语言模块与矩阵已接入，但旧业务页面仍有未迁移文字。词条检查统计仅覆盖已登记词条，不等于全站翻译完成。新增语言列也不等于全站已经有对应译文。

## 5. 复现命令

项目根目录（使用实际项目 Python 环境）：

```bash
python -m pytest backend/tests/test_localization.py backend/tests/test_localization_matrix.py backend/tests/test_indicator_graph.py -q
```

`frontend` 目录：

```bash
npm run test -- --run src/i18n src/components/localization src/pages/LanguageTerminology.test.tsx --silent --reporter=dot
INDICATOR_TEST_PYTHON=/path/to/project/python npx playwright test e2e/localization.spec.ts --workers=1
npm run i18n:check
npm run build
npx tsc --noEmit --project tsconfig.json
```

## 6. 文档与版本控制

《语言与术语中心_i18n详细设计.md》顶部已补充矩阵实施说明，覆盖早期单语言表格设想。CodeGraph 已同步。

显式 17 路径的 AI 路由覆盖检查中，Header 已覆盖，16 个新模块/配置路径未覆盖；新增模块尚未 Git 跟踪。未为通过检查而暂存文件，也未改动现有并行路由 JSON 草稿。后续纳入版本控制时需同步模块路由登记。

本次保留工作区原有及并行开发改动，未提交、推送或部署。
