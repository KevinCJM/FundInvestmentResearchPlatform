# SAA / TAA 开发分支提交记录

日期：2026-09-13。开发分支 `codex/saa-taa-frontier-grid` → `Dev`；更新现有 [PR #14](https://github.com/KevinCJM/FundInvestmentResearchPlatform/pull/14)，本轮不合并。

## 当前范围

1. 大类分类、投资目标与前瞻 CMA、不可变长期政策、TAA 主动风险门禁及训练/留出分段验证。
2. 同一前沿工作区保留单项/联合约束、逐轮投影随机游走、收益/风险选择、0.1%/0.5%等权重精度及分桶选种；恢复功能没有被三个代表点精炼替代。
3. 整条前沿默认 20、可选 200 个收益目标，单点预算默认 300 次，两者独立；固定签名 NJIT 主动集 QP / BFGS-SQP。连续参考曲线、离散权重采用和局部精炼的口径明确；共同候选集合统一重建 Pareto 前沿及代表点。
4. 投资目标增加资金计划、基准目标、资产及组授权、CMA 诊断、历史只读和 SAA 消费；修复本次审核确认的资本门槛回灌不一致、时钟变化后旧候选仍可采用两个 P2。

详细修复与证据见 [修复复核报告](../../investment-mandate-fix-recheck-2026-09-13.md)，要求见 [投资目标优化](investment-mandate-optimization-2026-09-13.md)，技术设计见 [投资目标设计](investment-mandate-research-design-2026-09-13.md) 和 [前沿恢复设计](frontier-restoration-design-2026-09-13.md)。

## 分支与保留

原 PR head 为 `2e5cdcfad4bb954f695f35b8266d68b9fc9c78ff`。已将最新 `Dev=f77f47d42e377e705485e6bb689e4784e8b46ba4` 合并到同一开发分支，再仅迁入明确任务范围。远端已有修复和最新治理规则保留；当前原工作目录未切分支、重置、清理或暂存其他任务。

原始审核证据保留。用户此前明确接受的 [第五轮 P2](../../saa-taa-recheck-round5-2026-09-13.md) 仍存在：重复暴露形成奇异协方差时 QP 端点可能耗尽预算，网格返回 `range_unresolved`。这不是不可行证明，也没有被本轮两个 P2 的关闭所豁免或抹去。

## 本轮验证与提交流程

最终提交目录的相关后端 520 项（481+39）、前端全量 957 项通过；真实隔离 API / NJIT 的桌面及手机 10 项通过，另有模拟 API 的旧 TAA 浏览器流程 2 项通过。原始两条缺陷复现与 96 组独立资金递推对照通过。TypeScript、生产构建、设计检查、i18n通过；详细结果、范围与补充回归在修复复核报告统一记录。

按本分支已同步的当前 [提交规范](../../branch_submission_rules.md) 执行：开发分支提交和推送后更新 PR；不沿用旧规范已撤销的 `ai-review` / `quality-gate` 三项门禁阻塞。本次仍执行独立源码及增量组合审核，最终 HEAD / base 与结果绑定在 PR。

不提交正式数据、缓存或构建产物；截图仅为离线夹具验收。回滚通过新的开发分支、`git revert` 和 PR，不在源码保留旧实现副本。历史验收文档中的“未提交”“路由待纳入”属于当时记录，以本轮复核和 PR 实际状态为准。
